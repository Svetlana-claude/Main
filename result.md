# Текущий ход работ

Файл читается в начале каждой сессии. Здесь — на чём остановились, что в работе,
что осталось.

---

## Состояние на 09.09.2026

Репозиторий создан и связан с GitHub, но **пуст по содержанию** — кода нет, это
заготовка под будущие проекты. Профиль работ: преимущественно **дизайн и вёрстка**.
Выбор стека в каждом проекте — на усмотрение исполнителя, фиксируется в `architect.md`.

### Сделано

- Настроен SFTP-доступ к `/home/mokeeva/main`; стартовый каталог переключён на
  `exchange/` (проверено: `pwd` даёт `/home/mokeeva/main/exchange`, shell-вход не сломан).
- Включён беспарольный `sudo` — установка пакетов больше не блокирована.
- Разобрано ТЗ первого проекта, ответы получены. Архитектура: веб-интерфейс поверх
  Claude Code в неинтерактивном режиме, PostgreSQL 16, nginx, FastAPI. Подробности —
  `exchange/tz_090926.md` и `exchange/ant.md`.
- Разобран `CLAUDE.md`, расхождения сняты (см. `exchange/quest_090926.md`).
- Заведены файлы сопровождения.
- Инициализирован git, ветка `main`, добавлен `.gitignore` — `exchange/` не версионируется.
- Подключён remote `origin` → `git@github.com:Svetlana-claude/Main.git`,
  ветка `main` отслеживает `origin/main`, первый push выполнен.
- SSH-ключ для GitHub добавлен на аккаунт `Svetlana-claude`, доступ проверен.

### В работе

**23.09.2026. Тату (`tatu/`): змея — изгибы тела и чешуя.** Просили сделать эскиз
реалистичнее по образцу двух примеров. На поверку «эскиз» оказался готовой
фотореалистичной работой, распечатанной и наклеенной на плечо в студии, а не
наброском. Перерисовать нечем: генератора изображений нет и на сервер он не
встанет — видеокарты нет, ОЗУ 2 ГБ. Выдан разбор с заданием на доработку —
`tatu/zmeya-dorabotka.md`, PDF в `downloads/` (5 страниц со снимками): чем тело
на эскизе отличается от примеров, что именно из них взять (блик на каждой
чешуйке, ряды под 35° к оси, смена размера чешуи поперёк тела, отражённый свет
по нижнему краю витка), задание на композицию, поправки на заживание кожи и
готовый запрос для нейросети в img2img. Открытый вопрос — куда уходит нижний
виток, к грудине или по рёбрам: от него зависит размер работы. Рядом лежит
`tatu/zmeya-varianty.html` — векторный построитель змеи из первого захода;
жанр не тот (контур против фотореализма), оставлен до решения.

**22.09.2026. Лодка 8–12 м (`lodka/`): подготовка к инвесторам.** Составлен свод
`lodka/prezentaciya-dlya-investorov.md`. Там разобраны удачные примеры (Candela,
Navier, Arc, Vision Marine) и провал X Shore, перечислено, что должно быть готово
до презентации (испытания, патент, спрос, экономика, сертификация, команда),
описан комплект материалов, питч-дек на 16 слайдов, трудные вопросы и чек-лист.
Дальше нужно наполнить его данными самой лодки: суть изобретения, замеры,
себестоимость. Есть и в PDF (9 страниц A4) — `lodka/downloads/prezentaciya-dlya-investorov.pdf`,
собирается `lodka/make_pdf.py`.

**24.09.2026. SkyCenter: ещё три варианта сертификата и подбор шрифтов.** Заказчик
выбрал по стилю вариант B «Высота» (сверено по md5 с присланным файлом), поэтому
светлый близнец собран на той же разметке — модификатор `.b-light`, вариант D
«Высота. День». Рядом два варианта с другим тоном: E «Допуск» — инженерный бланк
на IBM Plex с таблицей данных, подписью инструктора и печатью; F «Борт» — авиагоризонт
с лентой высоты, узкий гротеск Fira. Всё на `maket/sertifikat-varianty.html`,
копия с ресурсами — `downloads/maket-varianty/`, превью снимает `maket/shot.py`.
Шрифты: шесть наборов на одной строке сертификата с разбором, что каждый обещает
читателю; рекомендация — IBM Plex на документах обучения, Unbounded оставить
подарочной линии. Записка `sertifikat-varianty.md`, PDF на 5 полос в `downloads/`.
Решения за заказчиком: какой вариант в тираж, делить ли шрифт на «подарок» и
«документ», плашечный оранжевый для F — занесены в `current_questions.md`.

**25.09.2026. Письмо-микс: тренажёр, обучение, тандемы, сертификаты.**
Две редакции сведены в одну и разложены по пунктам — 13 разделов
с подпунктами. Главные четыре: **VR-тренажёр** (что это, что отрабатывается,
почему работает, чем ещё обеспечена безопасность), **обучение** (первый
самостоятельный прыжок таблицей — «Лесник», крыло, 1200 м, стабилизация,
4 часа теории, рация, самолёты; курс из семи занятий до удостоверения;
инструкторы поимённо), **тандемы** тремя типами **S / M / L** как они
названы на сайте, и **сертификаты** бронза / серебро / золото — таблицей
соответствия к тандемам плюс отдельный сертификат на самостоятельный прыжок.
Фактическая часть снята с сайта целиком: найден `sitemap.xml`, разобраны
девять страниц, вынуты тексты и 105 адресов снимков. Фотографий стало 17,
**все разные** — прежний набор повторял один кадр тренажёра в двух обрезках.
Новое: второй кадр VR-зала, отделение со стабилизирующим парашютом, три
тандемных кадра под пакеты (без съёмки / оператор рядом / камера на штанге),
портрет старшего инструктора, аэродром летом с воздуха. PDF — 16 полос,
проверены постранично. Открытых вопросов девять, в `current_questions.md`.

**24.09.2026. Письмо переработано под VR-тренажёр и под продажу.** Наземная
подготовка идёт на **VR-тренажёре** — это главный довод центра, поэтому он
вынесен в первый раздел, а фото шлема стоит сразу под подзаголовком.
Фотографии теперь свои, с сайта заказчика `главпрыг.рф` (домен живёт только
в зоне `.рф`, сайт на Tilda, адреса снимков — в `data-original`): тренажёр,
занятие с инструктором, тандем, свободное падение, купол, аэродром с высоты,
возвращение с поля. Заглушки с чужих аэродромов оттуда ушли; с Викисклада
остались только исторические кадры. Текст переписан как продающий: что именно
отрабатывается на тренажёре и почему «столько раз, сколько нужно», меры
безопасности, раздел с пятью частыми возражениями, лестница пакетов
с допродажей съёмки, лучшее время для записи и призыв к звонку; цены
в печатный бланк не вынесены — тираж живёт дольше прайса. По сайту уточнена
матчасть: первый самостоятельный прыжок — система **«Лесник» с 1200 м**,
4 часа теории, радиосопровождение (догадка про «Арбалет» убрана). В `pdf.css`
добавлены `figure.polnaya` (88 мм без кадрирования — лента `shirokaya`
отрезала головы) и `figure.vstup` (62 мм для заставки). PDF — 12 полос A4,
проверены постранично. Открытых мест пять, все в `current_questions.md`;
самое важное — **два имени на одном аэродроме**: материалы с `главпрыг.рф`,
а подпись и телефоны SkyCenter.

**24.09.2026. Письмо переписано под линейку сертификатов.** Теперь оно покрывает
все программы: тандем тремя пакетами (бронзовый / серебряный / золотой —
отличаются только съёмкой), «Арбалет» (первый самостоятельный прыжок) и AFF.
Вторым разделом идёт тренажёр — наземная подготовка как отличие центра, с
разбором того, что на нём отрабатывается, и перечнем остальных мер безопасности.
В конце — шесть историй парашютистов (Котельников, Минов, Киттингер, Андреев,
Баумгартнер, Рут Ларссон) и шесть фотографий с Викисклада: только общественное
достояние и CC0, скачиваются `foto/skachat.py`, лицензии — `foto/istochniki.md`.
PDF на 9 страниц. Что требуется от заказчика (название программы, решение по
пакетам, подробности и фото тренажёра, свои снимки) — в `current_questions.md`.

**22.09.2026. Сопроводительное письмо к сертификату SkyCenter.**
`sc-parashyuty/soprovoditelnoe-pismo.md`, PDF — `downloads/soprovoditelnoe-pismo.pdf`
(4 страницы, фирменное оформление). Восемь разделов по пунктам: сертификат (год,
не именной), кто может прыгать (возраст, вес до 120 кг, противопоказания, алкоголь),
как записаться, во что одеться, как пройдёт день (девять шагов от регистрации до
ссылки на видео), правила, соглашение сторон, контакты аэродрома. Сведения взяты
с сайта заказчика — разделы FAQ и «Тандем». Соглашение сторон дано существом, с
пропусками под реквизиты исполнителя; выверить его должен юрист заказчика — вопрос
занесён в `current_questions.md`.

**22.09.2026. Шрифты макетов отданы в TTF.** В `sc-parashyuty/downloads/ai/fonts/`
— 13 статических начертаний (те же, что в вёрстке), лицензии OFL, образец
`shrifty-obrazec.pdf` и архив `downloads/shrifty-skycenter.zip`. Собирает
`maket/fetch_fonts.py` из вариативных исходников google/fonts, образец печатает
`maket/fonts_sample.py` — он же служит проверкой установки.

**22.09.2026. Выгрузка `.ai` проверена векторным редактором.** На сервер поставлен
Inkscape (Adobe Illustrator поставить нельзя — проприетарная программа для
Windows/macOS). Файлы открываются, текст редактируемый, имена слоёв кириллицей
записаны верно. Слои в них понимают Illustrator и Acrobat, Inkscape — нет;
выдать вдобавок SVG со слоями не вышло, конвертеры теряют либо заливки, либо
текст (подробности в `architect.md`). Если правка пойдёт в Inkscape, выгрузку
нужно будет собирать иначе — вёрсткой листа сразу в SVG.

**22.09.2026. Документы по проектам выдаются в PDF.** Markdown заказчик не читает,
поэтому в `downloads/` теперь лежит PDF. Собирает общий `tools/md2pdf.py`
(Markdown → печатные стили → headless Chromium). Переведены все выдачи:
`sc-parashyuty` (в фирменном оформлении — шрифты и цвета макета, `pdf.css`),
`bezopasnost`, `webui`. Правило записано в `CLAUDE.md` пунктом 16: поправил
документ — пересобери PDF.

**22.09.2026. SkyCenter: выгрузка макетов в Illustrator.** Три варианта выгружены
в `sc-parashyuty/downloads/ai/*.ai` — по 15 слоёв в файле (пять изделий × фон,
графика, текст), слои гасятся по одному. Собирает `maket/export_ai.py` прямо из
`index.html`: печатает лист по слою за проход и складывает страницы в PDF с
группами необязательного содержимого. Текст — настоящий текст: шрифты переведены
в статические начертания (`maket/static_fonts.py`), иначе Chromium встраивал их
как Type 3 и править буквы было нельзя. Рядом лежат шрифты для установки
(`maket/fetch_fonts.py`) и записка `sc-parashyuty/vygruzka-ai.md` с оговорками:
вылетов нет, цвет RGB, контурные цифры варианта B и серебро — вручную.

**21.09.2026. SkyCenter (`sc-parashyuty/`): гайдлайн и сертификаты.** Собраны
4 референса (Skydive Dubai, Red Bull, Virgin Galactic, GoPro) и разобран текущий
стиль сайта; предложена основа гайдлайна — `sc-parashyuty/referensy-firmennogo-stilya.md`.
Сделаны 3 варианта стиля: сертификат, конверт и книжка парашютиста —
`sc-parashyuty/maket/index.html`, превью в `downloads/variant-*.png`. Рекомендация:
A — основной стиль, B — премиальная серия. Ждём выбора; дальше — гайдлайн
(знак, охранное поле, сетка) и печатные файлы с вылетами под выбранный вариант.

**VPN-сервер (`vpn-server/`, `7194304`).** Комплект WireGuard готов: установщик,
выдача и отзыв клиентов, проверка `tests/check-clients.sh`, описание с разбором
отказов. Пакеты поставлены, логика выдачи и отзыва прогнана на поддельном
`/etc/wireguard` и падает на трёх дефектах, которые обязана ловить.

**Сервер поднят 12.09.2026:** `46.8.178.196:51820/udp`, туннель `10.8.0.1`, служба
`wg-quick@wg0` активна и в автозапуске. Клиенты `notebook` и `phone` заведены,
конфиги лежат в `exchange/vpn/` (там же `phone-qr.png` для телефона).

Управление идёт через обёртку `webui-vpn` (`cbb6bd0`) — агенту разрешена
беспарольно, набор команд закрытый: `install | add | remove | list | status`.
Работает она с root-овой копией скриптов в `/usr/local/lib/webui-vpn`, поэтому
правка `vpn-server/*.sh` вступает в силу только после повторного
`sudo bash infra/install-root-helpers.sh`.

**12.09.2026. Панель VPN во вкладках рабочего места.** Между «Проекты» и
«Настройки» — вкладка «VPN»: завести клиента, показать QR-код, скачать `.conf`,
отозвать доступ, увидеть трафик и время последней связи по каждому клиенту.
Числа обновляются сами раз в 10 с. Всё идёт через ту же обёртку `webui-vpn`,
в `/etc/wireguard` приложение не ходит. Чтобы это заработало, нужны два шага
с вашей стороны: «Настройки» → «Перезапустить приложение» (маршрута `/vpn`
до перезапуска нет) и `sudo bash infra/install-root-helpers.sh` (без новой
копии обёртки таблица клиентов пуста — трафик отдаёт команда `peers`).

**Остались две проверки, доступные только с клиента:** доходит ли UDP через
брандмауэр хостера и идёт ли трафик через туннель (на «мой IP» должно стать
`46.8.178.196`). Пока они не пройдены, считать VPN рабочим нельзя.

**12.09.2026. Проект 3: ночной аудит безопасности (`bezopasnost/`).** Разобрана
инструкция `security-control.md`, устройство подчинено той же модели доступа,
что у VPN: код аудита под root в `/opt/secaudit`, состояние под пользователем
в `/var/lib/secaudit`, между панелью и root — обёртка `webui-sec` с закрытым
набором команд и **без единого пути в доводах**. Замысел целиком, включая
вкладку «Безопасность» в интерфейсе, — `bezopasnost/plan-bezopasnost.md`.

Сделаны вехи 1 и 2: скрипты (`collect`, `diff`, `remediate`, `apply-firewall`,
`audit`, `config`, `CONTEXT.md`) и обёртка с установкой. Проверки —
`bezopasnost/tests/`, обе прогнаны на дефектах и на каждом падают.

**Снятая картина «до» (`bezopasnost/sostoyanie-do.md`) дала три находки:**
сервер не обновлялся с установки (354 пакета в очереди, из них **197
security**, журнал `unattended-upgrades` пуст, ядро стартовое для 24.04);
**вход по SSH паролем разрешён**; `fail2ban` и `iptables-persistent` не
установлены. Первое и третье чинятся раньше самого аудита.

**Состояние на 14.09.2026.** Сделано и проверено делом: все 350 обновлений
встали (кроме ядра — нужен `dist-upgrade`), автообновления включены, fail2ban с
джейлом `recidive` работает (676 банов за двое суток), SSH ужесточён
(`permitrootlogin no`, `maxauthtries 3`, `AllowUsers mokeeva`), вкладка
«Безопасность» в меню, ночной аудит в расписании, карантин проверен пробой.
Выгрузки базы, терявшиеся 11–12.09, снова идут.

**14.09.2026, после перехода на ключи — агент ведёт сам.** Пароль по SSH
выключен и проверен попыткой входа, полный доступ получен. Журнал SSH прочитан:
чужих успешных входов нет. Файрвол default-deny действует (подтверждён, не
закреплён), эталон принят, все четыре приёмочные пробы аудитом найдены,
`dist-upgrade` поставлен. Остались вопросы 1–3 в `current_questions.md`: второй
фактор на вход в панель, новый вход по SSH и время перезагрузки ради ядра,
уборка старого ключа и копий sudoers.

**14.09.2026, завершение.** Файрвол закреплён (доступ по SSH снаружи подтверждён
вашим входом в 05:29), старый ключ и копии sudoers удалены, пароль root сменён
(`root-пароль.txt` в «Файлах проекта»), в панель добавлен второй фактор, сервер
перезагружается ради ядра `6.8.0-139`. Второй фактор включается вами:
«Настройки» → «Второй фактор входа».

**15.09.2026. Итог ручного прогона на странице (`2316ac0`).** Прогон с панели
не обрывался из-за ухода со страницы — он идёт на сервере и длится 15–25
секунд. Но после конца страница молчала, и это выглядело как обрыв. Теперь под
кнопками виден итог последнего прогона по `audit.log` (время, длительность,
ссылка на отчёт или текст ошибки), а перезапуск службы из «Настроек»
дожидается идущего прогона. Проверка — `webui/tests/check_sec_run_result.py`.

**14.09.2026. VPN и Безопасность — темами «Веб-интерфейса».** Проекты «VPN
сервер» и «Безопасность» убраны из раздела «Проекты». Их диалоги «Создание»
перенесены в проект «Веб-интерфейс» темами «VPN сервер» (14 сообщений) и
«Безопасность» (36), продолжаются с того же места. Туда же перешли два
загруженных файла, а «Файлы проекта» бывшей «Безопасности» — в папку
`bezopasnost/` внутри выдачи «Веб-интерфейса». Каталоги `vpn-server/` и
`bezopasnost/` в репозитории остались: это исходники вкладок «VPN» и
«Безопасность». Копия базы до переноса —
`/home/mokeeva/backups/webui-before-merge-20260914.sql.gz`.

**14.09.2026. Разбор расхода токенов — `webui/rashod-tokenov.md`** (копия в
«Файлах проекта»). За всё время $163.92 по оценке CLI, две трети — тема
«Безопасность». 57 % — перечитывание контекста на каждом шаге: тема живёт одной
сессией, окно 1 млн, автосжатие не срабатывало ни разу, контекст дорос до 690 тыс.
Ещё ≈ 26 % — полная перезапись кэша после простоя больше часа (ответ из 3–4
шагов — $7). ≈ 100 тыс. токенов контекста — файлы, заново подложенные CLI при
каждом `--resume`. Проверено пробником: `CLAUDE_CODE_AUTO_COMPACT_WINDOW=100000`
в `-p --resume` сжимает сам, ручное `/compact` тоже работает. Предложения и их
цена — в документе.

**14.09.2026. Потолок контекста и сжатие — сделано и работает.** Служба
перезапущена 15.09.2026 в 06:27 (Москва). Проверено: маршрут сжатия отвечает
(401 без входа вместо 404), `claude` получает потолок 250 000, и первое сообщение
темы «Токены» после перезапуска началось с автосжатия — 321 тыс. → 12 тыс. токенов.
Теперь:
- в «Настройках» — «Потолок контекста, токенов», по умолчанию 250 000: дорастая
  до него, Claude Code сам сжимает историю темы (по моделированию «Безопасность»
  стоила бы $52 вместо $113);
- в «Ходе работы» виден размер контекста — по ходу, в итоге и в простое; у
  крупной темы, остывшей больше часа, — предупреждение «кэш остыл»;
- у темы кнопка «Сжать контекст» — итог приходит в ленту сообщением со стоимостью.
  Темы выше потолка («Безопасность» 692 тыс., «Интерфейс» 320 тыс., «домен»
  310 тыс.) сожмутся сами в начале следующего сообщения — так было и в пробнике.
Правило 15 дополнено пунктом про `cp`; недоучёт токенов автосжатия записан
долгом 6 в `tech_debt.md`.

**14.09.2026. Исчерпанный лимит больше не выглядит «выполненным» — тоже после
перезапуска.** Когда Claude Code сообщает, что лимит кончился, «Ход работы»
пишет не «готово», а «Исчерпан лимит сессии · обнулится в 19:10 (Москва)» в
оформлении ошибки; сообщение в ленте красное; кружок темы красный, в подсказке —
время обнуления. После перезагрузки страницы строка «Хода работы» напоминает о
том же вместо «простой». Заодно исправлено: во время сжатия по кнопке кружок
темы горел зелёным — состояние искало запуск не под тем ключом. Вёрстка и
скрипт уже на диске, но подсказка кружка и строка простоя требуют перезапуска.

### Осталось

- ~~Перевести TLS на домен с Let's Encrypt~~ — **сделано 11.09.2026**, см. ниже.
- Периодически забирать копию базы из `exchange/backups/` по SFTP: копии лежат на
  том же диске, от его отказа они не спасают.
- Наполнять проекты по мере появления задач.

### Проект 1: webui — сделано

Все семь этапов пройдены, интерфейс работает по адресу `https://46.8.178.196/Claude`.
Первый вход: `Svetlana` / `admin`, смена пароля обязательна. Устройство описано
в `architect.md`.

**21.09.2026. Вход Claude из браузера.** 21.09 чаты перестали отвечать: истёк вход
Claude Code («OAuth session expired»), и починить его можно было только `/login` по
SSH. Теперь это «Настройки» → «Вход Claude»: аккаунт и срок входа (сейчас до
20.10.2026), «Проверить», «Войти заново» — ссылка на claude.com, код, проверка. Если
вход истёк, в шапке любой страницы плашка «Войти заново», а в чате вместо сырой
ошибки — объяснение. Служба перезапущена, страница проверена нажатиями на рабочей
службе (до ввода кода — настоящий вход не менялся). Устройство — `architect.md`,
«Вход Claude из браузера».

**12.09.2026. Часовой пояс.** В «Настройках» — выбор пояса, по умолчанию Москва
(UTC+3). Время во всём интерфейсе — лента, файлы, сессии, поиск, отчёты аудита,
выгрузка диалога, подписи скрипта — выводится по нему; «сегодня» на дашборде
считается с его полуночи. Хранится время по-прежнему в UTC. Заработает после
перезапуска службы. Устройство — `architect.md`, «Время и часовой пояс».

**12.09.2026. Правая колонка проекта с вкладками.** Колонка в полтора раза шире
(375 px), «Ход работы» расширился вместе с ней. Вверху две вкладки: «Загрузка» —
форма и загруженные файлы, «Файлы проекта» — папка выдачи со скачиванием и фильтром.
Окошко и кнопка «Файлы проекта» в верхней панели убраны. Выбранная вкладка помнится
между перезагрузками.

На сервере теперь работает headless Chromium (Playwright в отдельном окружении,
библиотеки поставлены через `webui-apt-install`) — интерфейс проверяется нажатиями
в настоящем браузере и снимками, а не только разбором разметки.

**15.09.2026. `webclaude.md` переписан, собрано «Руководство пользователя.docx».**
`webclaude.md` теперь — пошаговая инструкция для ИИ-агента: что спросить у человека,
что строится (разделы, база, драйвер, фоновые запуски, права, потолок контекста,
TOTP, `downloads/`, VPN, часовой пояс, аудит, SSH и sudo, перезапуск, дизайн) и
16 этапов развёртывания на пустом сервере с проверкой после каждого, без переноса
тем и чатиков. Руководство (24 страницы, 18 снимков) — интерфейс по разделам, работа
и настройка, установка (SSH-ключи на Windows/macOS/Linux, выключение пароля и
беспарольный sudo с откатом, файрвол с проверкой из второго окна, VPN, копии),
обслуживание, неполадки, словарь. Собирается скриптами `webui/manual/`:
`make_shots.py` поднимает отдельный стенд на вымышленных данных (своя база
`webui_demo`, заглушки движка, VPN и аудита; после съёмки всё удаляется) и снимает
экраны, `build_manual.py` пишет docx. В git — только скрипты; снимки и docx вне
версий, копии обоих документов — в `webui/downloads/`. Для проверки вёрстки docx на
сервер поставлены `libreoffice-writer-nogui` и `poppler-utils`; `python-docx` — в
`exchange/pwenv`.

**11.09.2026. Свод для повторения — `webui/webclaude.md`.** В один файл собрано
всё, что нужно, чтобы построить такой же интерфейс с нуля: стек и почему он такой,
структура репозитория и проекта, схема базы, устройство драйвера и фоновых запусков,
секреты, учёт расхода, дизайн-система, конфиги nginx и systemd, правила работы,
порядок сборки по вехам и список граблей. Файл положен в каталог проекта, а не в
корень: в корне остаются только файлы сопровождения (правило 10).

**11.09.2026. Адрес интерфейса — `https://mokeevasky.ru/Claude`.** Прежний
`https://46.8.178.196/Claude` работает, но по адресу браузер будет предупреждать
о подлинности: сертификат выдан на имена, а не на адрес. Закладку стоит переделать
на имя. Сертификат Let's Encrypt покрывает `mokeevasky.ru` и `www.mokeevasky.ru`
(с `www` уводит на основное имя), действует до 10.12.2026, продлевается сам
таймером `certbot.timer`. Пункт 1 техдолга закрыт.

**11.09.2026. Установка пакетов вошла в беспарольный набор** — обёрткой
`webui-apt-install`, которая принимает только имена пакетов. Сама команда
`apt-get install` с произвольными доводами не разрешена и не будет: через
параметр `-o ...::Pre-Invoke` и локальный `.deb` она равносильна полному root.

**10.09.2026.** Сам веб-интерфейс заведён в своём же разделе «Проекты» — проект
«Веб-интерфейс», каталог `/home/mokeeva/main/webui`, запуск команд разрешён.
Теперь его можно дорабатывать из браузера.

**11.09.2026, ключи под root.** Подготовлен перенос ключей в `/etc/webui/webui.env`
под root: `infra/secrets-to-root.sh` и врезка в юнит. Запуск за вами, нужен пароль.
Заодно список вычищаемых из окружения ключей задан явно — иначе после переноса он
опустел бы и `claude` снова наследовал бы ключи.

**11.09.2026.** `SECRET_KEY` и пароль базы вынесены из каталога проекта в
`~/.config/webui/webui.env`. Одного переноса не хватило: файл читался и по новому
пути, а ещё наследовался дочерним `claude` прямо в окружении — добавлены запрет
по пути и вычистка окружения. Пункт 5 техдолга закрыт; остаток (команда `cat` при
поднятом `allow_bash`) ушёл в пункт 2.

**10.09.2026.** В разделе «Настройки» появилась кнопка «Перезапустить приложение» —
правки кода применяются, не выходя из браузера. Перезапуск ставится в очередь и ждёт
завершения выполняющихся ответов, иначе остановка службы оборвала бы их вместе
с проделанной работой. Закрыт пункт 4 техдолга.

**10.09.2026, окно диалога.** Убрана ошибка «Сбой обработки: Separator is not found…»,
которая дважды срывала работу над прокруткой: вывод движка читался с буфером в 64 КиБ,
а движок кладёт в одну строку JSON и целый файл, и вывод команды. Теперь поток
разбирается построчно без ограничения на длину (`267c673`).

Там же приведено в порядок само окно диалога (`1f0de48`): страница занимает ровно окно,
лента прокручивается внутри себя, поле ввода стоит внизу. Промежуточные действия
(«инструмент: Bash») выведены из ленты в отдельный блок «Ход работы» — правая колонка
в «Проектах» делится надвое: сверху «Файлы», снизу ход работы со своей прокруткой.
В информационной строке блока — таймер, счётчик шагов и расход токенов, после
завершения время, стоимость и итог. Живой расход требует перезапуска службы: он
берётся из драйвера.

Проверки живут в `webui/tests/` и запускаются как `.venv/bin/python tests/<файл>.py`;
`pytest` в проекте нет. Каждая перед сдачей прогонялась на прежнем, сломанном коде —
и падала на нём.
