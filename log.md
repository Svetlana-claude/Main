# Журнал задач

Новые записи добавляются **вверх** списка.

---

## 09.09.2026 08:58 — Веб-интерфейс: реализация всех этапов

**Статус:** выполнено
**Коммиты:** см. ниже

**Суть задачи:** пройти все семь этапов плана из `exchange/tz_090926.md` — от установки
инфраструктуры до раздела «Настройки».

**Ход:**
- Этап 0. Установлены PostgreSQL 16, nginx 1.24, окружение Python. Создана база и роль
  `webui`, самоподписанный сертификат на IP, конфиг nginx с TLS и ограничением частоты
  входа, юнит `webui.service` в автозапуске. Проверено снаружи: `https://46.8.178.196/Claude`
  отвечает, HTTP уводит на HTTPS.
- Этап 1. Схема из девяти таблиц, применяется идемпотентно при старте. Пароли argon2id,
  серверные сессии, обязательная смена пароля при первом входе, проверка стойкости,
  защита от перебора. Сценарий проверен 11 шагами: от отказа на слабом пароле до
  блокировки после серии неудач.
- Этап 2. Дизайн-система в духе 1С на CSS-токенах, светлая и тёмная темы, компоненты:
  командная панель, таблицы, формы, плитки метрик, метки, уведомления.
- Этап 3. Дашборд: процессор, память, диск, нагрузка, сеть, аптайм, процессы;
  автообновление раз в 10 с с настраиваемым интервалом; график за сутки на canvas;
  учёт расхода токенов. Числа сверены с `free -m`, `df` и `/proc/loadavg` — совпадают.
- Этап 4. Чатики: список, создание, переименование, удаление, поток ответа через SSE,
  автоназвание по первой реплике, продолжение диалога через `--resume`.
- Этап 5. Проекты: темы, диалог с доступом к каталогу проекта, файлообмен.
  Проверено реальной задачей — агент создал и отредактировал файл.
- Этап 6. Настройки: параметры приложения с проверкой значений, редактор файлов
  сопровождения с предпросмотром, активные сессии, права проектов.
- Идеи 1-6 реализованы: предупреждение о месячном пределе, полнотекстовый поиск по
  диалогам, экспорт в markdown, `log.md` лентой задач, тёмная тема, график истории.

**Найдено и исправлено при проверке нажатием:**
- `--allowed-tools` не запрещает остальные инструменты — Bash выполнялся при
  `allow_bash=false`. Добавлен явный `--disallowed-tools Bash BashOutput KillShell`.
  Повторная проверка: команда отклонена, правка файлов сохранилась.
- Модель бралась произвольным первым ключом `modelUsage` — в базу попадал `haiku`
  вместо `opus`. Теперь берётся модель с наибольшим числом токенов на выход.
- Slug проекта сохранял кириллицу, каталог получался `/home/mokeeva/main/проба`.
  Добавлена транслитерация.

## 09.09.2026 08:25 — Проект 1: веб-интерфейс для совместной работы

**Статус:** разбор выполнен, реализация ожидает подтверждения
**Коммиты:** —

**Суть задачи:** разобрать ТЗ из `exchange/tz_090926.txt`, изложить план работ, вопросы
и идеи в `exchange/tz_090926.md`, ждать ответов.

**Ход:**
- Собрано состояние сервера: Ubuntu 24.04, 4 CPU, 2.9 ГБ RAM, 41 ГБ свободно;
  из нужного установлен только Python 3.12.3, порт занят лишь 22.
- Цены и модели Claude API, а также возможности по лимитам токенов сверены со
  справочником, а не по памяти.
- Составлен план: 7 этапов от инфраструктуры до раздела «Настройки», с критериями
  готовности по каждому.
- Выявлены три блокера: нет прав на установку пакетов, пароль уходит по открытому HTTP,
  нужен API-ключ Claude. Записаны в `tz_090926.md` с вариантами решения.
- Уточнений по ТЗ — шесть, включая расхождение «три раздела» против четырёх
  перечисленных и глубину раздела «Проекты».
- Получены ответы заказчика. Встречные вопросы (достаточно ли подписки Pro; зачем
  нужен SDK, если задача — заменить терминал) разобраны в `exchange/ant.md`.
- По итогам разбора архитектура пересмотрена: вместо Messages API через `anthropic` SDK
  движком становится сам Claude Code в неинтерактивном режиме — задача не «ещё один
  чат», а веб-пульт к терминалу. Факты по Agent SDK и аутентификации сверены с
  документацией code.claude.com, не по памяти.
- СУБД по решению заказчика — PostgreSQL 16 вместо MariaDB.
- Заказчик выбрал вариант А (веб-интерфейс поверх Claude Code в неинтерактивном режиме),
  подтвердил личный характер инструмента без сторонних пользователей.
- Включён беспарольный `sudo` — блокеры 3.1 и 3.2 сняты, этап 0 разблокирован.

## 09.09.2026 07:38 — Первичная настройка репозитория

**Статус:** выполнено
**Коммиты:** `b702fd0` — первичная настройка, `c27b149` — хэш в log.md, `b5c5ece` — remote на GitHub

**Суть задачи:** дать SFTP-доступ к рабочей папке, разобрать `CLAUDE.md`, завести
файлы сопровождения, инициализировать git и подготовить подключение к GitHub.

**Ход:**
- Починен SFTP: на сервере отсутствовал ed25519 host-ключ, из-за чего клиент FAR
  (движок PuTTY) не мог согласовать host key алгоритм и падал на «Connecting to host».
  Выполнен `ssh-keygen -A`, ключи перечитаны после перезапуска `ssh.socket`/`ssh.service`.
- Стартовый каталог SFTP переведён на `exchange/` через
  `Subsystem sftp internal-sftp -d %d/main/exchange` — правка применена 09.09.2026 08:53,
  после включения беспарольного `sudo`; бэкап конфига
  `/etc/ssh/sshd_config.bak-090926-085327`. Проверено: `pwd` даёт
  `/home/mokeeva/main/exchange`, `cd ..` работает, shell-вход не затронут.
- Вопросы по `CLAUDE.md` заданы в `exchange/quest_090926.md`, ответы получены.
- Заведены файлы сопровождения: `architect.md`, `result.md`, `current_questions.md`,
  `tech_debt.md`, `log.md`.
- Инициализирован git, добавлен `.gitignore`.
- Сгенерирован ключ для GitHub, публичная часть выложена в `exchange/git.txt`.
- `CLAUDE.md` приведён в соответствие с полученными ответами.
- Подключён remote `origin` (`git@github.com:Svetlana-claude/Main.git`), доступ по SSH
  проверен (аккаунт `Svetlana-claude`), первый push выполнен.
