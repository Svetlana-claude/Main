# Журнал задач

Новые записи добавляются **вверх** списка.

---

## 26.09.2026 — SkyCenter: письмо переставлено — тандем первым, таблицы не рвутся

**Статус:** выполнено

**Суть задачи:** из пункта 1.1 убрать первую фотографию, где человек без
VR-очков; не делить пункт 2.1 «Первый самостоятельный прыжок» между полосами —
все таблицы должны читаться блоком, целиком на одной странице. Поставить
тандем первым пунктом как важное вступление: сначала показать тандем —
ознакомительный прыжок с инструктором, потом расписать самостоятельный прыжок
и курс обучения, затем мотивирующие и успокаивающие пункты про безопасность
и инструкторов и всё остальное.

**Ход.**

1. **Снимок убран.** `vr-zanyatie.jpg` (зал, участница в подвесной системе без
   шлема) выведен в запас — рядом с крупным планом очков он читался «не про
   VR». В разделе о тренажёре осталось два кадра: очки и подвеска.
2. **Таблицы больше не рвутся.** В `pdf.css` добавлен запрет
   `table { break-inside: avoid }`: в базовых стилях `md2pdf.py` защищена от
   разрыва только строка `tr`, поэтому таблица про самостоятельный прыжок
   уезжала шапкой на третью полосу, а хвостом на четвёртую. Проверено по
   прежней сборке: до правки разрыв виден, после — все восемь таблиц письма
   стоят целиком.
3. **Порядок переставлен по пути читателя.** Раздел 1 — тандем как
   ознакомительный прыжок (как устроено, три сертификата, что важно знать),
   раздел 2 — самостоятельный прыжок и курс из семи занятий, раздел 3 —
   «Почему это безопасно: тренажёр и инструкторы» (VR, что отрабатывается,
   почему работает, чем ещё обеспечена безопасность, кто вас поведёт).
   Дальше без изменений: допуск, запись, день, вопросы, правила, соглашение,
   истории, контакты. Вступление переписано под новый порядок, перекрёстные
   ссылки на разделы выверены.
4. **Концовка подтянута на полосу с контактами:** таблица контактов сведена
   с шести строк до четырёх, подпись об источниках снимков укорочена. PDF —
   12 полос, проверен постранично.

**Коммит:** `04c791a`

---

## 25.09.2026 — SkyCenter: письмо сжато, курс по сайту, тандемы и сертификаты вместе

**Статус:** выполнено

**Суть задачи:** переработать в письме пункт 2.2 «Курс до удостоверения» по
информации об обучении с сайта главпрыг.рф; пункт 2.3 «С кем вы будете
заниматься» сделать без фотографий; в разделе о тандемах вместо трёх типов
сделать золото / серебро / бронзу, где золото — тандем с видеооператором плюс
съёмка с руки инструктора (всё включено), серебро — видео только с руки
инструктора, бронза — без видео; раздел о тандемах переформатировать, фото
внутри текста убрать, оставить одно красивое в начале; соединить сертификаты
и тандемы; сохранять краткость и раскладывать информацию на максимально
компактные блоки, чтобы письмо было коротким, но очень информативным.

**Ход.**

1. **Курс переписан по сайту.** Взята страница `/education`: семь занятий
   таблицей — приезд и инструктаж с VR-тренажёром на первом, труба на пятом,
   смена системы и экзамены на шестом, выпуск на седьмом. Добавлено то, что
   сайт обещает отдельно: индивидуальный график и удостоверение, с которым
   принимают на дропзонах по всему миру.
2. **Инструкторы — таблицей без фотографий.** Портрет Киселёва из письма
   убран, четыре имени с опытом стоят таблицей «кто / опыт».
3. **Тандемы и сертификаты соединены в раздел 3.** Одно фото в начале
   (оператор снимает тандем со стороны), внутри текста снимков нет. Лестница
   названа по-заказчиковски: бронза — без видео, серебро — видео с руки
   инструктора, золото — оператор плюс камера инструктора. Названия сайта
   S / M / L из письма убраны; расхождение с прайсом сайта («Тандем M» —
   это видеооператор, а не рука инструктора) записано в `current_questions.md`.
4. **Письмо сжато.** «Что вы отработаете» и «инструкторы» стали таблицами,
   запись и одежда сведены в один раздел, вопросы и правила подрезаны.
   Разделов 11 вместо 13, фотографий 13 вместо 17 — четыре ушли в запас
   (`foto/istochniki.md`, скрипт скачивает их по-прежнему).
5. **PDF пересобран и проверен постранично** — 12 полос вместо 16. Два пустых
   низа убраны: тандемное фото уменьшено до обычного размера, аэродром
   в контактах поставлен лентой на всю полосу, подпись об источниках снимков
   подтянута на полосу с контактами (раньше занимала отдельную).

**Коммит:** `dc5ae97`

---

## 25.09.2026 — SkyCenter: письмо-микс, тренажёр, обучение, тандемы, сертификаты

**Статус:** выполнено

**Суть задачи:** взять сопроводительное письмо редакций 1 и 2 и сделать
правильный микс. Нужны фотографии про тренажёр — **разные, не повторять одни
и те же**. Главное содержание: инфо по тренажёру, инфо по обучению, описание
тандемов (какие типы), описание сертификатов — золото, серебро, бронза.
Изложить чётко и по пунктам.

**Ход.**

1. **Повтор подтвердился.** `vr-trenazher.jpg` оказался обрезкой того же
   кадра, что и `vr-instruktor.jpg`, — один момент занятия стоял в PDF дважды.
   Обрезка из набора убрана.
2. **Сайт разобран целиком, а не одной страницей.** Нашёлся `sitemap.xml`
   и 19 адресов; сняты `/`, `/tandem`, `/arbalet`, `/education`, `/sport`,
   `/fly`, `/prig`, `/user`, `/photo`, из них вынуты и текст, и все адреса
   снимков (105 штук). Кандидаты отсмотрены контрольным листом, а не по
   именам файлов: из 40 тандемных кадров выбраны три разных сюжета — без
   съёмки, оператор рядом, камера на штанге.
3. **Фотографий стало 17, все разные.** Новые: второй кадр VR-зала
   (`vr-zanyatie.jpg`), отделение со стабилизирующим парашютом
   (`stabilizaciya.jpg`, слева срезана чужая подпись), три тандемных под
   пакеты, портрет старшего инструктора, летняя аэрофотосъёмка аэродрома.
4. **Факты выверены по сайту.** Тандем — 60 секунд свободного падения, 14+,
   до 120 кг, приезд за час, съёмка только штатная. Самостоятельный —
   «Лесник», крыло, 1200 м, стабилизация, ~5 секунд падения, 4 часа теории,
   рация, Ан-28 / Ан-2 / L-410 / Ми-8. Обучение — семь занятий до
   удостоверения, с трубой и сменой системы; инструкторы поимённо
   (Киселёв 5000+, Красавин 2000+, Шалошик, Юльцев). Тандемные пакеты на
   сайте называются **S / M / L** — подарочная линия бронза / серебро /
   золото положена на них таблицей соответствия.
5. **Письмо пересобрано в 13 разделов** с подпунктами: тренажёр (1.1–1.4),
   обучение (2.1–2.3), тандемы (3.1–3.2), сертификаты, кто может прыгать,
   запись, одежда, день, частые вопросы, правила, соглашение, истории,
   контакты. PDF — 16 полос A4, проверены превью постранично; в `pdf.css`
   добавлен `figure.portret` (42 мм), иначе портрет инструктора занимал
   пол-полосы.

**Коммит:** `3feddc3`.

**Что осталось у заказчика** — девять пунктов в `current_questions.md`:
цена «Тандема L», высота тандема, длительность занятия в VR и марка шлема,
нужна ли медсправка тандемщику, есть ли AFF на деле, и два имени с тремя
разными телефонами на печатной выдаче.

---

## 24.09.2026 — SkyCenter: письмо под VR-тренажёр, свои фотографии, продающая правка

**Статус:** выполнено

**Суть задачи:** в сопроводительном письме подготовка идёт на **VR-тренажёре** —
это надо отразить. Фотографии взять со своего сайта `главпрыг.рф` и вставить
в документ; фото VR-очков поставить сразу после подзаголовка о наземной
подготовке. VR — фишка центра, нужен акцент. Само письмо проработать
как профессиональный менеджер по продажам.

**Ход.**

1. **Сайт нашёлся не сразу.** `главпрыг.ру` и `glavpryg.ru` не разрешаются;
   живёт домен `главпрыг.рф` — в пуникоде `xn--80aebc1b0ah2g.xn--p1ai`. Сайт
   на Tilda, снимки подгружаются лениво, адреса лежат в `data-original`
   и отдаются только с браузерным User-Agent. Выбраны восемь кадров, каждый
   сопоставлен со своим блоком на сайте — так VR-снимки доказуемо именно
   VR-снимки, а не «что-то похожее».
2. **`foto/skachat.py` переписан** под два источника: свои снимки с Tilda
   и только исторические кадры с Викисклада. Часть исходников — PNG, поэтому
   всё приводится к JPEG шириной 1400 px через Pillow: снимки попадают под
   `.gitignore` и не раздувают PDF. Три прежние заглушки с чужих аэродромов
   из списка убраны (с диска не удалялись — правило 1).
3. **Письмо переписано целиком** как продающий документ. VR-тренажёр вынесен
   в **первый** раздел, фото очков стоит сразу под подзаголовком, как
   и просили. Дальше — что именно отрабатывается (отделение, счёт, раскрытие,
   управление куполом, заход на цель, действия в особых случаях,
   приземление), довод «столько раз, сколько нужно», меры безопасности
   с радиосопровождением и вывод «вы платите не за адреналин, а за то, чтобы
   адреналин был безопасным». Добавлен раздел с пятью частыми возражениями
   («а если не раскроется», «а если передумаю в дверях», «боюсь высоты»,
   «не в форме», «а если погода»), лестница пакетов с допродажей съёмки,
   «лучшее время» для записи и призыв к звонку. Цены в печатный бланк
   намеренно не вынесены — тираж живёт дольше прайса.
4. **Матчасть уточнена по сайту:** программа первого самостоятельного прыжка
   идёт на системе **«Лесник» с 1200 м** со стабилизацией свободного падения,
   4 часа теории и радиосопровождением. Прежняя догадка про «Арбалет»
   из письма убрана.
5. **Вёрстка PDF доведена по полосам.** Лента `figure.shirokaya` (кадр 52 мм
   с обрезкой) отрезала головы на вертикальных снимках — заведён
   `figure.polnaya` (высота до 88 мм, без кадрирования); для заставки под
   заголовком — `figure.vstup` (62 мм), иначе первая полоса на 40 % пустовала.
   Снимок тренажёра снят из-под подвесной системы и без пояснения читается
   перевёрнутым — добавлена подпись. Два горизонтальных кадра в разделе 6
   опущены до обычных 72 мм: ушли пустые низы полос 5–7, а сноска
   о фотографиях перестала занимать отдельную тринадцатую полосу.
   Итог — 12 полос A4, проверены превью постранично.

**Коммит:** `07f8b49`.

**Что осталось у заказчика** (записано в `current_questions.md`): доступен ли
VR тандемщикам, сколько длится занятие и как называется тренажёр, нужен кадр
своего занятия вместо крупного плана шлема, права и печатное разрешение
снимков с сайта, и главное — **два имени на одном аэродроме**: материалы
с `главпрыг.рф`, а подпись и телефоны SkyCenter. На печатной выдаче должно
остаться одно имя и один набор телефонов.

---

## 23.09.2026 — Тату: змея с изгибами и чешуёй

**Статус:** выполнено (с оговоркой — см. ниже)

**Суть задачи:** эскиз тату со змеёй сделать реалистичнее — добавить телу красивые
изгибы и чешую по образцу двух примеров. Композицию сохранить, но допустимо дать
4 варианта расположения тела.

**Ход, часть 1 (без картинок).** Исходники в первых двух заходах до сессии не дошли,
и шелла не было. Сделана независимая от эскиза часть: `tatu/zmeya-varianty.html` —
параметрический построитель змеи в SVG (осевая линия сплайном Катмулла — Рома,
чешуя рядами с полуячейковым сдвигом, подбрюшные щитки, голова, перехлёсты с
подложкой; 4 расположения, 3 стиля).

**Ход, часть 2 (картинки нашлись).** Снимки лежали в `webui/uploads/` — три файла
от 23.09 11:55. Разобраны, скопированы в каталог проекта под внятными именами;
с примеров 2 и 3 обрезан интерфейс телефона (`primer-*-chisto.jpg`).

⚠️ **Постановка на поверку оказалась не та.** «Эскиз» — не набросок, а готовая
фотореалистичная чёрно-серая работа, распечатанная и приклеенная на плечо в
студии. Перерисовать её нечем: инструмента генерации изображений нет, на сервер
он не ставится — видеокарты нет, ОЗУ 2 ГБ. Вектор из части 1 — другой жанр
(контур против фотореализма) и на эту задачу не работает; файл оставлен, судьба
его — вопрос к заказчику.

Вместо перерисовки выдан разбор и задание: `tatu/zmeya-dorabotka.md`, PDF —
`tatu/downloads/zmeya-dorabotka.pdf` (5 страниц, со встроенными снимками,
оформление `tatu/pdf.css`). Содержание: три причины плоского вида тела (лента
ровной ширины, нет перехлёста, чешуя одного размера и контраста), что именно
взять из примеров (серповидный блик на каждой чешуйке с долями тонов, ряды под
35° к оси, смена размера чешуи поперёк тела, отражённый свет по нижнему краю
витка, подбрюшные щитки), задание на композицию, поправки на кожу и заживание,
готовый запрос для нейросети в режиме img2img на русском и английском.

PDF проверен постранично картинками (`pdftoppm` + `montage`), а не фактом сборки.

---

## 24.09.2026 — SkyCenter: ещё 3 варианта сертификата и подбор шрифтов

**Статус:** выполнено
**Коммиты:** `c878954`

**Суть задачи:** заказчик выбрал по стилю вариант B «Высота» (прислал его в
загрузке). Сделать ещё три варианта подарочного сертификата, один из них — почти
в том же стиле, но на светлом фоне. Отдельно предложить варианты шрифтов
с учётом серьёзной и «безопасной» темы обучения парашютным прыжкам.

**Ход.** Присланный файл сверен по md5 с `downloads/variant-b.png` — совпал, то
есть выбран именно вариант B, и его разметка взята за основу светлого близнеца.

Сделано три варианта на `maket/sertifikat-varianty.html`:

- **D «Высота. День»** — та же разметка, что у B, плюс модификатор `.b-light`:
  светлая бумага, фольга тёмной частью градиента, новые горизонтали
  `img/contours-navy.svg` (строка в `gen.py`, остальные картинки не изменились).
- **E «Допуск»** — инженерный бланк на IBM Plex: тёмный корешок, таблица данных,
  строки подписи инструктора и даты, круглая печать. Наземная подготовка вынесена
  отдельной строкой.
- **F «Борт»** — авиагоризонт: небо и земля, линия горизонта со знаком самолёта,
  лента высоты справа с оранжевым указателем на 4000. Узкий гротеск Fira Sans
  Condensed, числа — Fira Mono.

Шрифты: `maket/fetch_web_fonts.py` догрузил 40 файлов woff2 (cyrillic + latin)
в `maket/fonts/` и собрал `fonts-varianty.css`; на странице шесть наборов на
одной и той же строке сертификата с разбором, что каждый обещает. Рекомендация —
IBM Plex на документах, Unbounded оставить подарочной линии.

Превью снимает новый `maket/shot.py` (Playwright снимает элемент, а не окно).
Записка `sertifikat-varianty.md` → `downloads/sertifikat-varianty.pdf` (5 полос),
копия страницы с ресурсами — `downloads/maket-varianty/`.

**Две правки по ходу.** У варианта D фольга легла плашкой поверх имени: короткая
запись `background` в `.b-light .foil-text` сбрасывала `background-clip: text` —
заменено на `background-image`. У варианта F небо и земля встали в поток: правило
`.f-cert > *  { position: relative }` перебивало `position: absolute` у самих
слоёв — заменено перечислением содержимого.

Шрифты в карточках проверены не на глаз, а через CDP
(`CSS.getPlatformFontsForNode`): все шесть наборов резолвятся в заявленные
семейства — «засечки» у Inter на уменьшенном превью оказались артефактом
масштабирования.

---

## 24.09.2026 — SkyCenter: письмо под линейку сертификатов, тренажёр и истории

**Статус:** выполнено
**Коммиты:** `f23487f`

**Суть задачи:** переписать сопроводительное письмо с учётом того, что продаётся:
сертификаты на тандем (пакетами — золотой, серебряный, бронзовый), сертификаты на
AFF и разовый прыжок с теорией и подготовкой на тренажёре («Лесник»; название под
вопросом, возможно «Арбалет»). Тренажёр подчеркнуть особо — это отличие центра и
признак серьёзного отношения к безопасности, а безопасность для клиента решает
всё. Добавить истории известных парашютистов и найти фотографии.

**Ход:** письмо перестроено под три программы. Пакеты сверены с сайтом: там уже
есть «серебряный» (22 500 ₽, прыжок) и «золотой» (30 000 ₽, со съёмкой),
бронзового нет — предложена лестница, где пакеты отличаются **только съёмкой**,
а прыжок во всех одинаковый. Цены в письмо не вынесены: бланк живёт дольше
прайса.

Название программы разбиралось по матчасти, а не на слух: «Лесник-3» сделан
в 2000 году на базе «Арбалета-2» и предназначен парашютистам-пожарным — то есть
сомнение заказчика верное, «Лесник» и правда ведомственное имя. Взят «Арбалет»,
вопрос вынесен в `current_questions.md` вместе с просьбой подтвердить, на каких
куполах программа идёт на самом деле.

Тренажёру отдан отдельный раздел вторым номером, сразу после линейки: подвесная
система, макет двери, что именно отрабатывается, допуск к прыжку только после
зачёта, и рядом — чем ещё обеспечена безопасность (два парашюта, страхующий
прибор, техосмотр, допуски инструкторов, метеоограничения для новичков).
Написан он общими для любой дропзоны словами: своих подробностей нет, они
запрошены.

Шесть историй в конце: Котельников (ранец РК-1 после гибели Мациевича), Минов
и Мошковский (26 июля 1930, Воронеж), Киттингер (31 333 м и перчатка, о которой
он не доложил), Андреев (24 500 м свободного падения, рекорд на полвека),
Баумгартнер (панические атаки в скафандре, пять месяцев с психологом — и
сверхзвук) и Рут Ларссон (тандем в 103 года). Все цифры сверены по Википедии,
Guinness World Records и сайту заказчика.

Фотографии — шесть, с Викисклада, `foto/skachat.py`. Отбирались **по лицензии**:
только общественное достояние и CC0, ни одной CC BY-SA — та требует указания
автора и share-alike, что для бланков заказчика неудобно. Свободной фотографии
Котельникова нет (на Викискладе только мемориальные доски под CC BY-SA), взята
юбилейная марка. Сами `.jpg` в git не кладутся (правило 2), скачиваются
скриптом; состав и лицензии — `foto/istochniki.md`.

Вёрстка снимков — `figure` в `pdf.css`. Первая попытка дала подпись во всю
полосу под узким вертикальным кадром, вторая (`display: table`) — обрывок
подписи в колонку шириной с кадр; оставлен первый вариант с подписью по центру.
Проверено постранично картинками: 9 страниц, снимки с подписями не рвутся.

---

## 22.09.2026 — SkyCenter: сопроводительное письмо к сертификату

**Статус:** выполнено
**Коммиты:** `89b3bcc`

**Суть задачи:** написать сопроводительное письмо для того, кому подарили прыжок:
подготовка, как проходит прыжок, правила, ободряющие слова для новичка. Кратко и по
пунктам, чтобы удобно перечитывать. Обязательно — соглашение сторон и вся нужная
информация.

**Ход:** факты собраны с сайта заказчика (skycenter.su, разделы FAQ и «Тандем»), а не
придуманы: возраст с 18 лет или с 14 при родителе, вес до 120 кг, справка не нужна,
список противопоказаний, одежда и обувь, начало лётного дня 09:00 летом и 10:00 зимой,
высота 3000–4000 м, инструктаж 10–15 минут, фото и видео ссылкой в WhatsApp, срок
сертификата год и он не именной, записываться заранее не нужно — достаточно звонка.
Письмо — `sc-parashyuty/soprovoditelnoe-pismo.md`, восемь разделов: сертификат, кто
может прыгать, как записаться, во что одеться, как пройдёт день (девять шагов),
правила, соглашение сторон, контакты. Соглашение дано существом, с пропусками под
реквизиты исполнителя и подписи: предмет, обязанности обеих сторон, принятие риска,
перенос и отмена, персональные данные. Ободряющие слова — в начале и в конце, а
середина сухая, чтобы её было удобно перечитывать по пунктам.

PDF собран по правилу 16 (`tools/md2pdf.py --css pdf.css`) и проверен постранично
картинками, а не фактом сборки: у двух таблиц шапка была пустой и печаталась тёмной
полосой без текста — заменена на осмысленную («Сторона / Сведения», «Куда / Как
связаться»).

---

## 22.09.2026 — Шрифты макетов отдать в TTF

**Статус:** выполнено
**Коммиты:** `556231e`

**Суть задачи:** собрать в выдачу все шрифты, использованные в макетах SkyCenter,
в формате TTF — ровно те начертания, что стоят в вёрстке.

**Ход:** `maket/fetch_fonts.py` переписан: вместо шести вариативных файлов качает
их как исходник и нарезает 13 статических начертаний по списку из `fonts.css`
(Oswald 500/700, Manrope 400/600/800, JetBrains Mono 400/700, Caveat 500,
Unbounded 400/600/800, Rubik 900 и 900 курсив), плюс лицензии OFL и архив
`downloads/shrifty-skycenter.zip`. Имена начертаний проставляются вручную:
`updateFontNames` берёт их из таблицы STAT, а там описаны не все веса — на
Caveat 500 он падает. Биты «курсив / полужирный / обычный» в `fsSelection`
взаимно исключают друг друга, гасить надо все три сразу.

Проверка по делу, а не сборкой: шрифты поставлены в систему (`fc-cache`), все 13
видны с правильными семейством и стилем; `maket/fonts_sample.py` печатает
образец **системными** шрифтами без `@font-face` — подмена была бы видна сразу.
Образец нашёл две ошибки. Первая настоящая: JetBrains Mono вставал как
«JetBrainsMono» без пробела, то есть не совпал бы с именем в вёрстке. Вторая —
в самой оснастке (правило 13): все строки набрались одним весом, потому что
двойные кавычки внутри HTML-атрибута `style` обрывали его; шрифты были ни при
чём. После правок в PDF-образце 12 разных встроенных шрифтов, кириллица на месте.

---

## 22.09.2026 — Проверить выгрузку .ai в настоящем векторном редакторе

**Статус:** выполнено
**Коммиты:** `e9b35e5`

**Суть задачи:** поставить на сервер векторный редактор и проверить им файлы `.ai`
(Adobe Illustrator поставить нельзя — проприетарная программа для Windows/macOS).
Ставится Inkscape; задача — убедиться, что слои видны редактору, и поправить
выгрузку, если нет.

**Ход:** поставлен Inkscape 1.2.2 из apt. Что он показал: `.ai` открывается, текст
остаётся текстом, кириллические имена слоёв внутри файла записаны правильно
(UTF-16 с BOM — проверено по байтам, иначе Illustrator показал бы кракозябры).
Но слои из групп необязательного содержимого Inkscape не понимает: разворачивает
лист одним слоем, а выключенные слои всё равно рисует.

Попытка выдать вдобавок SVG со слоями (15 слоёв → Inkscape → одна сборка,
82 секунды на все три варианта) отменена: конверсия теряет работу. Inkscape
переводит градиенты в вырожденные картинки 1×1 с маской и часть плашек не рисует
— у сертификата пропали тёмная шапка и текстура поля; `pdftocairo -svg` рисует
верно, но весь текст переводит в кривые (ноль элементов `<text>`), то есть
редактировать нечего. Код сборки SVG откачен, проба перенесена в
`exchange/sc-parashyuty-svg-proba/`. В записке для дизайнера добавлен раздел
«Чем открывать»: `.ai` и `.pdf` — Illustrator и любая читалка, Inkscape слоёв
не покажет.

---

## 22.09.2026 — Записки по проектам выдавать в PDF, а не в Markdown

**Статус:** выполнено
**Коммиты:** `817d19a`

**Суть задачи:** заказчик не читает `.md`. Документы по проекту нужно отдавать в PDF
(или другом векторном формате). Перевести выдачу `sc-parashyuty` в PDF.

**Ход:** общий `tools/md2pdf.py` (Markdown → печатные стили → headless Chromium),
чтобы не плодить копии `lodka/make_pdf.py` по проектам. Проектное оформление
подключается отдельным файлом: `sc-parashyuty/pdf.css` — шрифты и цвета макета.
Собраны PDF по всей выдаче: `sc-parashyuty` (2), `bezopasnost` (4), `webui` (2).
Правило записано в `CLAUDE.md` пунктом 16.

Две ошибки, обе видны только в готовом файле: `@import` проектного css действовал
только в самом начале стилей, поэтому фирменные шрифты молча подменялись на DejaVu
(поймано `pdffonts`, после правки — Oswald и Manrope); блоки кода ``` схлопывались
в одну строку без расширения `fenced_code`. Страницы просмотрены после каждой
правки: таблицы не едут, ссылки живые, оранжевые маркеры на месте.

---

## 22.09.2026 — SkyCenter: выгрузка макетов в .ai по слоям

**Статус:** выполнено
**Коммиты:** `b635c6c`, `f824817`

**Суть задачи:** выгрузить три варианта макетов в формате Illustrator (.ai) с разбивкой
по слоям, чтобы дизайнер мог их править.

**Ход:** `maket/export_ai.py` собирает выгрузку из `index.html` — второго макета не
заводится. Пять изделий варианта раскладываются на общий лист, лист печатается по
одному слою за проход (фон, графика, текст для каждого изделия), страницы складываются
pikepdf в PDF с группами необязательного содержимого; файл сохраняется как `.ai`.
Итог — 15 слоёв в каждом из трёх файлов, `downloads/ai/`.

Ошибки, найденные проверками, а не глазом: лист печатался с белым фоном и верхний слой
закрывал всё под собой (слои переключались, а картинка не менялась — снимки совпадали
побайтно); подстановка в правила слоя шла `replace` вместо `format`, и в каждый слой
попадал весь макет; заливка самого изделия в слое графики затирала плашки под собой;
гашение цвета заходило внутрь `svg` и стирало рисунки, залитые `currentColor`.

Отдельно — шрифты. `pdffonts` показал Type 3 почти везде: такой текст в Illustrator не
редактируется. Причина воспроизведена на отдельной странице в четыре строки —
вариативные woff2 от Google Fonts (`fvar`/`gvar`) Chromium встраивает как Type 3,
статический Caveat вышел нормальным. `maket/static_fonts.py` вырезает из каждого файла
нужный вес; после этого все шрифты вариантов A и C — `CID TrueType`. Осталось
исключение: контурные цифры «4000» варианта B (`-webkit-text-stroke`) печатаются
кривыми всегда. Страница проверена в браузере — консоль чистая, вёрстка не поехала.
`maket/fetch_fonts.py` кладёт в выдачу TTF и лицензии OFL, записка для дизайнера —
`sc-parashyuty/vygruzka-ai.md`.

---

## 22.09.2026 — Лодка: свод для инвесторов в PDF

**Статус:** выполнено
**Коммиты:** `b211d9b`

**Суть задачи:** пересохранить `lodka/prezentaciya-dlya-investorov.md` в PDF.

**Ход:** Скрипт `lodka/make_pdf.py`: Markdown → HTML с печатными стилями → PDF через
headless Chromium. Итог — 9 страниц A4, шрифт DejaVu Sans встроен, внизу нумерация,
флажки чек-листа печатаются квадратиками для отметки. Проверено снимками страниц:
таблицы и списки не поехали, разметка Markdown в текст не просочилась. PDF лежит в
`lodka/downloads/` и в git не входит.

---

## 22.09.2026 — Лодка 8–12 м: что нужно для презентации инвесторам

**Статус:** выполнено
**Коммиты:** `340e32a`

**Суть задачи:** в роли предпринимателя-изобретателя расписать всё необходимое для
успешной презентации инвесторам проекта нового типа лодки длиной 8–12 м; найти
в интернете примеры удачно презентованных инновационных проектов (лодки/катера)
и по пунктам свести, что нужно.

**Ход:** Найдены и разобраны Candela (90 писем фондам → ангел → прототип → 129 млн €),
Navier (брони с задатком, показ на CES), Arc (раунд A через год, выход в буксиры),
Vision Marine (показ на воде), X Shore (банкротство из-за завода раньше спроса),
данные DocSend о том, что читают инвесторы, и шаблон Sequoia. Итог записан в
`lodka/prezentaciya-dlya-investorov.md`.

---

## 21.09.2026 — SkyCenter: сертификат, конверт, книжка парашютиста — 3 варианта

**Статус:** выполнено
**Коммиты:** `c7f8fc3`

**Суть задачи:** по итогам разбора референсов сделать в едином стиле подарочный
сертификат, подарочный конверт и книжку парашютиста — три варианта оформления на выбор.

**Ход:** Сделан макет `sc-parashyuty/maket/index.html`: HTML/CSS в миллиметрах,
новый знак (крыло-купол, стропы, парашютист-точка). Варианты: A «Рейс SC-4000»
(талон, конверт «авиапочта», книжка-паспорт), B «Высота» (тёмный премиум, фольга,
шкала высоты), C «Свободное падение» (вид сверху, фигура, оранжевая лента, прогресс
до лицензии). Проверено в Chromium на ширине 1400 и 390: консоль чистая, все
шрифты загружены, страница не едет по горизонтали. По скриншотам исправлены
перфорация марки, наезд «4000» на имя, фигура парашютиста и обрезанная лента.
Копия макета и PNG-превью лежат в `downloads/`.

---

## 21.09.2026 — SkyCenter: 4 референса фирменного стиля для сертификатов

**Статус:** выполнено
**Коммиты:** `6b4018c`

**Суть задачи:** под будущий гайдлайн и дизайн сертификатов SkyCenter (skycenter.su)
собрать 4 известных фирменных стиля из парашютного спорта, авиации и экстрима:
что в них работает и что взять для SkyCenter.

**Ход:** Из CSS сайта сняты цвета (`#30CFFA` основной) и шрифты (Bebas Neue + Lato).
Разобраны Skydive Dubai, Red Bull, Virgin Galactic и GoPro. Итог и предложенная основа
гайдлайна (палитра, знак, линейка сертификатов «посадочный талон») лежат в
`sc-parashyuty/referensy-firmennogo-stilya.md`, копия — в `downloads/`.

---

## 21.09.2026 — Вход Claude из веб-интерфейса

**Статус:** выполнено
**Коммиты:** `6615d98`, `bed9065`

**Суть задачи:** повторить `/login` Claude Code из браузера, чтобы истёкший вход не
требовал SSH. «Настройки» → «Вход Claude»: состояние входа и срок, кнопка «Войти
заново» (ссылка на claude.com → код → подтверждение), проверка вызовом `claude -p`.
В чате ошибку истёкшего входа заменить понятным сообщением со ссылкой на настройку.

**Ход:** `services/claude_auth.py` запускает `claude auth login --claudeai` в
псевдотерминале, вынимает ссылку из гиперссылки OSC 8, пишет код в терминал, после
выхода проверяет `claude -p`. Драйвер ставит признак «вход не работает» по ошибке
входа (stderr и итог с `is_error`) — по нему плашка в шапке и объяснение в ленте.
Стенд `tests/check_claude_login.py` (поддельный `claude`, 41 проверка) нашёл дефект:
повторный вход после неудачного не получал ссылку — закрытый дескриптор терминала
оставался в цикле событий, а новый получал тот же номер. Исправлено. Проверка падает,
если убрать псевдотерминал, плашку или объяснение. На настоящем `claude` ссылка
получена целиком, вход отменён до кода. Служба перезапущена, страница пройдена
нажатиями в Chromium на рабочей службе (1280 и 400 px, консоль чистая).

---

## 21.09.2026 — Разбор: «не работает tmux и веб-интерфейс»

**Статус:** выполнено
**Коммиты:** — (правок в коде нет, только запись в журнале)

**Суть задачи:** выяснить, почему не работают tmux и веб-интерфейс.

**Ход:** Сервер, nginx, `webui`, PostgreSQL в порядке. Снаружи `https://mokeevasky.ru/Claude/login`
отвечает 200, адреса пользователя не забанены. Веб-интерфейс: с 12:08 на каждое сообщение
приходила ошибка `Failed to authenticate: OAuth session expired and could not be refreshed`,
то есть истёк вход Claude Code. После `/login` в 12:39 вызов `claude -p` отвечает,
`webui` работает от `mokeeva` с тем же `HOME`, так что новые данные входа подхватывает без
перезапуска. tmux: сам tmux исправен (тестовая сессия запускается), но сессий нет с
перезагрузки 14.09 05:45, а перезагрузку сессии tmux не переживают. Отмечено: 21.09 в
12:24–12:32 был вход под root с консоли хостера (tty1) и смена пароля `mokeeva`.

---

## 15.09.2026 — `webclaude.md` по текущему состоянию и «Руководство пользователя.docx»

**Статус:** выполнено
**Коммиты:** `e34067a`

**Суть задачи:** вернуться к ТЗ. Переписать `webclaude.md` под все изменения
интерфейса с 11.09 так, чтобы ИИ-агент по нему пошагово разворачивал такой же
интерфейс на пустом сервере (без переноса тем и чатиков). Сделать «Руководство
пользователя.docx»: описание интерфейса, работа и настройка, установка — SSH,
беспарольный доступ и прочие тонкости.

**Ход:** `webclaude.md` переписан как инструкция агенту: вопросы человеку и места
для подстановки, часть А — что строится (по разделам, с таблицами маршрутов, базы,
событий драйвера), часть Б — 16 этапов с проверками, часть В — правила, проверки,
грабли. Факты сверены с кодом и `infra/` (обёртки, пакеты, cron, имена выгрузок).
Для руководства написан `manual/make_shots.py`: отдельный экземпляр приложения на
базе `webui_demo`, поддельный движок (шаги, сжатие, исчерпанный лимит), заглушки VPN
и аудита, 18 снимков в Chromium, ключ TOTP на снимке размыт; стенд и база удаляются
после съёмки. `manual/build_manual.py` собирает docx (24 стр.), вёрстка проверена
отрисовкой через LibreOffice. Числа в тексте (10 попыток за 15 минут, сеанс 30 дней,
64 МБ, модели, транслитерация) сверены с кодом. Снимки и docx в `.gitignore`, копии
`webclaude.md` и docx положены в `downloads/` через `cp`.
По подтверждению записан техдолг № 7: время, дописанное скриптом, идёт с запятой,
а с сервера — без.

## 15.09.2026 — «Безопасность»: итог прогона не виден, прогон кажется оборванным

**Статус:** выполнено; Python подхватится после перезапуска службы
**Коммиты:** `2316ac0`

**Суть задачи:** пользователь нажал «Прогнать с устранением», ушёл на «Дашборд»,
вернулся — и по виду страницы решил, что прогон прервался из-за ухода.
Разобраться и сделать так, чтобы итог прогона был виден, откуда бы на страницу
ни вернулись.

**Ход:**
- По журналам прогон не прерывался: `POST /sec/run` в 04:10:51 UTC, в
  `audit.log` «готово» в 04:11:07, отчёт за 15.09 записан. Прогон идёт в
  процессе приложения, уход со страницы его не трогает. Дефект — в показе:
  после конца прогона надпись «Идёт прогон» гасла, и больше страница ничего не
  говорила; текст обещал «несколько минут», а прогон длится 16 секунд.
- `services/security.py`: `last_run()` — итог последнего прогона по
  `audit.log` («готово» за N с со ссылкой на отчёт, «ошибка» при пустом
  снимке или сбое запуска, «оборван» без «готово» дольше 30 минут, «идёт» —
  ночной). На странице — блок `#sec-last-run`.
- `app.js`: по концу прогона страница обновляется без `?ok=` — раньше после
  обновления снова висело «Прогон запущен».
- `services/restart.py`: перезапуск из «Настроек» ждёт и идущего прогона
  аудита — он в той же контрольной группе, вот там обрыв был бы настоящим.
- `tests/check_sec_run_result.py` (22 проверки, Chromium: нажать, уйти на
  «Дашборд», вернуться; остаться на странице). На прежнем коде — 8 провалов.
  Первый красный прогон был виной оснастки: стенд без nginx, а опрос шёл на
  `/Claude/sec/state` и получал 404 — стенду задан пустой `ROOT_PATH`.
- Попутно: `check_limit_time.py` оставлял в `/tmp` исполняемую заглушку
  `claude`; семь таких попали в ночной отчёт разделом MINER_IOC. Стенд теперь
  убирает за собой. `check_sec_panel.py`, `check_limit_time.py` — зелёные.
- Живой сайт до перезапуска: `/sec` отвечает 200 на новом шаблоне.

---

## 15.09.2026 — Размер контекста в базе; сжатие больших тем

**Статус:** в работе

**Суть задачи:** убедиться, что размер контекста темы пишется в базу, и сжать
темы, контекст которых выше потолка 250 тыс.: «Безопасность», «Интерфейс», «домен».

---

## 14.09.2026 — Техдолг по итогам сжатия; «готово» и зелёный кружок при исчерпанном лимите

**Статус:** выполнено; служба перезапущена 15.09.2026 03:27 UTC
**Коммиты:** `38d78eb`

**Суть задачи:** по согласию пользователя — записать в `tech_debt.md` недоучёт
токенов автосжатия; исправить ключ, по которому кружок темы узнаёт об идущем
запуске. Новое: когда токены кончились и ждём обнуления окна, «Ход работы»
показывает «готово», а кружок темы зелёный — так быть не должно.

**Ход:**
- Воспроизведено до правки: `tests/check_limit_state.py` (второй экземпляр,
  подставной `claude` отвечает итогом с `is_error` и строкой о лимите, Chromium
  жмёт «Отправить» и «Сжать контекст») на прежнем коде — 8 провалов: «готово»,
  зелёная строка, пузырь не ошибкой, кружок сразу зелёный, подсказка и строка
  простоя без лимита, во время сжатия сервер красит тему зелёным. Кружок
  краснел только через 10 с, по опросу.
- Причина «готово»: `app.js` обрабатывал `result` одинаково, не глядя на
  `is_error`. Теперь такой итог — `run.stop(text)` (строка ошибки без «сбой»),
  пузырь `msg--error`, кружок `work`.
- Причина зелёного при сжатии: `_topic_state` искал запуск под `project`, а
  заводятся они под `topic`. Ключ вынесен в `projects.RUN_KIND`, им же пользуется
  `check_topic_state.py` — на возвращённом старом ключе она падает («идущий
  ответ: done вместо work»).
- `projects._limit_note`: последняя запись — ошибка о лимите → подсказка кружка и
  строка простоя «Исчерпан лимит сессии · обнулится в …».
- Все проверки `tests/` зелёные; рабочая служба на старом Python с новым
  шаблоном отдаёт «Проекты» и «Чатики» с кодом 200.
- Долг 6 в `tech_debt.md` — недоучёт токенов автосжатия; вопрос «Расход токенов»
  из `current_questions.md` снят целиком.
- 15.09.2026: служба так и работала на коде от 14.09 11:19 UTC (маршрут
  `/compact` отвечал 404, размер контекста не писался) — за ночь тема «Токены»
  потратила $13.53, из них ≈ $2.8 на перезапись остывшего кэша. По просьбе
  пользователя перезапуск поставлен в очередь через `POST /settings/restart`
  (ждёт завершения этого ответа).
- Проверка после перезапуска: служба поднялась в 03:27:30 UTC;
  `POST /projects/topics/136/compact` без входа — 401, а не 404, значит маршрут
  есть; в окружении запущенного `claude` — `CLAUDE_CODE_AUTO_COMPACT_WINDOW=250000`.
  Первое же сообщение темы «Токены» началось с автосжатия: 321 329 → 12 433
  токенов (`compact_boundary`, trigger auto).

---

## 14.09.2026 — Разбор расхода токенов в веб-интерфейсе и способы его снизить

**Статус:** выполнено — разбор, по «продолжай» внедрены 3.1 и 3.2; нужен перезапуск службы
**Коммиты:** `be5f030` (разбор), `b93e7aa` (внедрение)

**Суть задачи:** при работе через веб-интерфейс расход токенов большой. Разобрать,
что и куда тратится, и предложить способ оптимизации.

**Ход:**
- База: расход по темам и сообщениям, цены подобраны регрессией по самой базе
  (чтение кэша ≈ $0.5/млн, запись ≈ $10/млн, выход ≈ $25/млн). Доли: чтение кэша
  57 %, запись 32 %, выход 10 %.
- Стенограммы: контекст «Безопасности» 18 тыс. → 692 тыс., ни одного
  `compact_boundary`; перезаписи кэша 600–684 тыс. после простоя; 51 повторное
  вложение изменённых файлов (≈ 314 тыс. знаков), из них 15 — копия
  `plan-bezopasnost.md` в `downloads/`.
- Пробники на haiku в `/tmp/compact-probe`: `CLAUDE_CODE_AUTO_COMPACT_WINDOW=60000`
  и `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=20` не сработали (контекст 65 тыс.
  остался), `=100000` при контексте 133 тыс. сжал на следующем `--resume`
  (`trigger: auto`, 19 тыс.); `claude -p /compact --resume` — 65 тыс. → 20 тыс.
  Отрицательный результат при 60000 — тот самый «дефект», на котором пробник
  показывает отсутствие сжатия.
- Моделирование потолков по стенограммам (без потолка $113.14 против
  фактических $108.77): при 250 тыс. «Безопасность» $52.53, «Интерфейс» $24.97.
- Документ `webui/rashod-tokenov.md` + копия в `webui/downloads/`; вопросы о
  внедрении — в `current_questions.md`. Код не менялся.

**Внедрение:**
- Драйвер: настройка `compact_window` (250 000, границы 100 000–1 000 000,
  0 — нет) → `CLAUDE_CODE_AUTO_COMPACT_WINDOW`, унаследованное значение
  убирается; события `compact` (start/done/failed, `too_few_groups` словами);
  `context_tokens` в `usage` и `result` по основному агенту.
- База: `conversations.context_tokens`, `context_at`; для 8 тем заполнены из
  стенограмм. Маршрут `POST /projects/topics/{id}/compact`: `/compact` фоновым
  запуском, итог — сообщение со стоимостью. Поле в «Настройках», размер
  контекста и «кэш остыл» в «Ходе работы», кнопка «Сжать контекст».
- Настоящий `claude` через драйвер (haiku): автосжатие на первом шаге отказало
  с `too_few_groups`, `/compact` сжал 131 тыс. → 2 тыс., следующий шаг 19 тыс.
- `tests/check_compact_context.py` — 11 утверждений, падает на пяти дефектах
  (не убирается унаследованная переменная, контекст от вспомогательного агента,
  контекст без кэша, без границ, не разбирается `compact_boundary`).
- `tests/check_compact_ui.py` — второй экземпляр с подставным `claude`, Chromium,
  нажатия «Сжать контекст», «Отправить», «Сохранить»; 22 утверждения. Падает на
  прежних `app.js` и `projects.html` и на лишней точке в итоге сжатия. Один
  красный прогон — вина оснастки (строка итога искалась не под тем номером),
  продукт был прав.
- Остальные проверки `tests/` зелёные. Рабочая служба на старом Python с новыми
  шаблонами: «Проекты», «Настройки», «Чатики» отвечают 200.
- Находки для техдолга и правило про `cp` — в `current_questions.md`.
- По решению пользователя правило 15 в `CLAUDE.md` дополнено пунктом про `cp`;
  находки для техдолга вынесены на обсуждение.

---

## 14.09.2026 — Проекты «VPN сервер» и «Безопасность» — темами «Веб-интерфейса»

**Статус:** выполнено
**Коммиты:** `2e36cd9` (только документация — перенос выполнен в базе)

**Суть задачи:** убрать из раздела «Проекты» проекты «VPN сервер» и
«Безопасность», завести в проекте «Веб-интерфейс» соответствующие темы и
перенести в них диалоги из тем «Создание» этих проектов.

**Ход:**
- До переноса — два пробника на одноразовых каталогах: `--resume` из чужого
  каталога находит сессию и дописывает стенограмму на старом месте; `Edit` вне
  `--add-dir` при флагах приложения проходит. Значит, стенограммы не трогаем
  (копия удвоила бы учёт расхода), а темы смогут править `../bezopasnost`.
  Первая проба `Edit` ничего не показала — в разбор JSON попало предупреждение
  CLI; повторена с `</dev/null`.
- Копия базы: `/home/mokeeva/backups/webui-before-merge-20260914.sql.gz` (600).
- Одной транзакцией: темы 63 и 72 → проект 5 с названиями «VPN сервер» и
  «Безопасность», файлы 2 и 3 → проект 5, проверка, что в проектах 20 и 23
  пусто, удаление проектов. Сообщения, `claude_session_id`, время не менялись.
- `bezopasnost/downloads/*` скопированы в `webui/downloads/bezopasnost/`;
  `vpn-server/downloads/` пуста. Каталоги проектов не удалялись.
- Код не менялся, перезапуск не нужен.

**Проверки.** Headless Chromium на рабочем сайте
(`exchange/check-merge-projects.py`), 13 утверждений: в списке только «Самолёт»
и «Веб-интерфейс»; щелчок по теме открывает её, в «Безопасности» 36 сообщений,
в «VPN сервер» 14; загруженные файлы во вкладке «Загрузка»; в «Файлах проекта»
выдача Безопасности, скачивание отдаёт 200 и 27 622 байта; старые `?id=20/23`
не роняют страницу; консоль чиста. Дважды красной её сделала оснастка — не
поднятый пул базы (пустая cookie) и селектор заголовка, бравший название
проекта; снимок подтвердил, что продукт прав. На возвращённом дефекте (проект
«VPN сервер» заведён снова, тема названа «Создание») проверка падает.

## 14.09.2026 — Время сброса лимита по часовому поясу

**Статус:** выполнено, заработает после перезапуска службы (поставлен в очередь)
**Коммиты:** `ecad3df`

**Суть задачи:** сообщение Claude Code «You've hit your session limit · resets
9:50am (UTC)» показывать со временем по часовому поясу из «Настроек», а не UTC.

**Ход:**
- Строку отдаёт сам Claude Code, приложение кладёт её в базу как сообщение с
  ролью «ошибка». CLI не поменять — переводится при показе, оригинал в базе
  остаётся: смена пояса переводит и старые сообщения.
- `timefmt.localize_limit`: ближайший момент сброса после прихода сообщения,
  дата — если сброс на другой местный день, недельный лимит с датой, переход
  через год. Незнакомое не трогается.
- **Первая версия перевода на экране ничего бы не изменила.** Роль «ошибка»
  пишется двумя путями — событием `error` и событием `result` с `is_error`.
  Перевод сперва встал только на первый. По полям пяти сохранённых сообщений о
  лимите (у всех есть модель и расход) видно, что все пришли вторым путём.
  Найдено до сдачи — стенд живой выдачи с заглушкой вместо `claude` падал бы.
- Стенд поймал ещё один дефект: `% 12` превращал «25:00am» в 01:00 — теперь
  час проверяется на 1–12 до пересчёта.

**Проверки.** `tests/check_limit_time.py`, 23 утверждения: функция, лента
чата, выгрузка, живая выдача через заглушку `claude` в формате настоящего CLI,
исходная строка в базе. Падает на пяти дефектах (3, 4, 2, 1, 2 провала). Все 12
стендов проекта проходят. Живая служба до перезапуска отдаёт «Чатики»,
«Проекты», «Настройки» — 200: шаблоны с `local_limit is defined`.

## 14.09.2026 — Уборка старого, пароль root, второй фактор, перезагрузка

**Статус:** выполнено, после перезагрузки проверено; второй фактор ждёт подключения
**Коммиты:** `e137fe3`

**Суть задачи:** удалить старый ключ и копии sudoers, сменить пароль root на
сгенерированный и передать, добавить вход по коду из приложения-аутентификатора,
перезагрузить сервер.

**Ход:**
- **Файрвол закреплён.** Основание — факт, а не предположение: в 05:29 UTC,
  уже при политике DROP, пользователь вошёл по SSH новым ключом с
  82.204.198.29. Значит порт 22 снаружи открыт. `netfilter-persistent save`:
  8 правил, DROP для v4 и v6.
- **Удалено:** ключ `sftp-access-mokeeva` из `authorized_keys` (перед этим
  проверено, что на него не ссылается `~/.ssh/config` и ничто в проекте),
  его закрытая часть на сервере и копия `authorized_keys` с ним — затёрты;
  три `010-mokeeva.bak-*` из `/etc/sudoers.d`. После — `visudo -c` чист,
  `sudo` работает, в `authorized_keys` один ключ.
- **Пароль root** сгенерирован (24 знака без похожих букв — вводить руками в
  консоли хостера) и записан сразу в `bezopasnost/downloads/root-пароль.txt`,
  ни разу не выведен на экран: вывод команд и ответы сохраняются в базе и её
  выгрузках. Хэш теперь `$y$` (yescrypt) вместо `$1$` (MD5); вход через `su`
  с новым паролем проверен.
- **Второй фактор** — см. `architect.md`, «Второй фактор входа». Стенд
  `check_totp.py`, 44 утверждения на своём пользователе `totp-probe`, падает на
  четырёх дефектах. При снятой проверке повтора в алгоритме живой вход код
  всё равно не пропустил — его остановил условный UPDATE: защита двойная.
- Живая служба до перезапуска проверена на шаблоне «Настроек», который уже
  подхватился с диска: 200, панель видна — условие `is defined` сработало.
- Перед перезагрузкой снят `/var/lib/secaudit/pre-reboot.txt` — ядро, службы,
  правила, — чтобы после старта сравнить.
- **После перезагрузки (05:45 UTC) сверено со снимком — 14 из 14 совпали:**
  ядро `6.8.0-139-generic`, восемь служб active, политика DROP и число правил
  v4/v6 те же (закрепление сработало), пароль по SSH выключен; упавших юнитов
  нет, признака «нужна перезагрузка» нет. Новый код входа живой: `/login/totp`
  без пропуска отвечает «Войдите заново», `/settings/totp` без сессии уводит
  на вход. fail2ban поднял `sshd` и `recidive`, баны пережили перезагрузку (677).
- Пароль root забран пользователем; `root-пароль.txt` затёрт (`shred -u`),
  поиск по домашнему каталогу и `/tmp` других копий не нашёл.

## 14.09.2026 — Вход на сервер только по ключам, агенту полный доступ

**Статус:** выполнено; ждёт перезагрузки ради ядра и вашего входа по SSH
**Коммиты:** `ee86d0e`, `34bb5b8`

**Суть задачи:** вход на сервер — только по SSH-ключам; у агента беспарольный
доступ ко всему, ставит и проверяет сам; вход в веб-интерфейс — логин и пароль.

**Ход:**
- Ключ `mokeeva-workstation-20260912` заведён 12.09, вход по нему проверен
  подключением с сервера, 14.09 — вами со своего компьютера. Прежний ключ в
  `authorized_keys` (`sftp-access-mokeeva`) оказался ключом, чья закрытая часть
  лежит на самом сервере: выключи пароль по нему — и вход с ноутбука не работал
  бы вовсе. Закрытая копия нового ключа из «Файлов проекта» затёрта.
- `infra/switch-to-keys.sh` — последний ручной шаг. Жёсткий порядок с
  проверкой делом между шагами: обёртки → ключи есть → пароль выключен →
  `sshd -T` и настоящая попытка входа паролем дают `(publickey)` → **только
  тогда** беспарольный `sudo`. Иначе, упади выключение пароля, машина осталась
  бы с беспарольным root при парольном входе — ровно то, что §11 инструкции
  называет недопустимым.
- Решение и что остаётся открытым записаны в `infra/sudo-allowed.list`: пароль
  панели теперь отделяет интернет от root (вопрос 1 в `current_questions.md`).
- **Попутно найден дефект отката файрвола.** Правила применялись без
  подтверждения, таймер отработал — IPv4 вернулся к ACCEPT, а IPv6 остался в
  DROP с новыми правилами: снимок «до» по IPv6 был пуст, восстановление из
  пустого файла ничего не сделало. Доступ не пострадал (правила полные), но
  откат, который откатывает не всё, создаёт ложную уверенность. Откат вынесен
  в `firewall-rollback.sh`, таймер ставится до применения, а не после.
- fail2ban за двое суток забанил 676 адресов — подбор идёт постоянно.

**Проверки.**
- `check-rollback.sh` — 8 утверждений; падает на прежнем откате (5 провалов)
  и на обратном порядке ACCEPT/очистки (1).
- `check-wrapper.sh` — 30; падает на снятой проверке «в режиме key без ключей
  не выключать пароль».
- Сборка sudoers с `ALL` прогнана копией `apply-sudo.sh`: `visudo` проходит,
  смесь `ALL` с перечнем и `ALL` в разделе postgres отвергаются.
- **Не проверено:** сам `switch-to-keys.sh` целиком — он требует root. Его
  шаги проверяются внутри него же, и он останавливается до выдачи доступа,
  если пароль не выключился.

**После `switch-to-keys.sh` (14.09.2026, агент сам, с полным доступом).**
- Проверено делом: `sshd -T` — `passwordauthentication no`,
  `kbdinteractiveauthentication no`; настоящая попытка входа паролем —
  `Permission denied (publickey)`; `sudo -n true` проходит.
- **Журнал SSH прочитан на признаки взлома** — раньше он был недоступен.
  Живая машина — с 08.09.2026 (до того только загрузки образа хостера в
  апреле 2024), так что root с MD5-паролем был открыт шесть дней, а не два
  года, как говорилось раньше. Успешные входы только с 82.204.198.29 (адрес
  пользователя), с 127.0.0.1 (проверки агента) и с 10.0.0.1 при сборке образа.
  Чужих успешных входов нет при 31 687 неудачных, из них 9 667 на root.
  Старым ключом `sftp-access-mokeeva` снаружи не входили ни разу.
- **Файрвол применён** с самооткатом. Прежде чем подтверждать, проверено:
  fail2ban банит в своей таблице nftables и от очистки INPUT не зависит; за
  25 с журналирования отброшенных пакетов ни одного на 22/80/443/51820 —
  режутся DHCP-запросы соседей (UDP 67) и сканирование; адрес статический,
  так что ловушки с продлением аренды DHCP нет. Подтверждён, но **не
  закреплён** (`firewall-save`): прямого доказательства доступности 22
  снаружи у агента нет, и до закрепления перезагрузка вернёт открытое
  состояние.
- **Эталон принят** после чтения снимка: SUID — 13 штатных, cron и юниты
  штатные плюс свои, ключей два и оба известны. Единственная «находка
  майнера» — собственная копия агента `/tmp/rb.good`; убрана до приёма.
- **Три приёмочные пробы §5 шага 9** (учётка, строка cron, SUID) найдены
  аудитом — каждая своей строкой ПОЯВИЛОСЬ; после уборки отчёт чист. Вместе
  с пробой карантина 12.09 — все четыре пройдены. Сверка «слушает наружу, но
  закрыто файрволом» при политике DROP промолчала — все четыре порта в правилах.
- **dist-upgrade** (30 пакетов) поставлен отдельной службой `systemd-run`, вне
  контрольной группы `webui`: иначе перезапуск приложения установщиком оборвал
  бы и сессию, и `apt`. Очередь пуста, службы живы, `dpkg --audit` чист. Ядро
  `6.8.0-139` установлено 13.09 и ждёт перезагрузки.

## 12.09.2026 — Часовой пояс в настройках, время везде по нему

**Статус:** выполнено, заработает после перезапуска службы
**Коммиты:** `be24b56`

**Суть задачи:** в «Настройках» — выбор часового пояса, по умолчанию Москва (UTC+3).
Время во всём интерфейсе выводить по выбранному поясу, а не по UTC.

**Ход:**
- Хранение не тронуто: сервер и база в UTC, переводится только показ. Модуль
  `app/timefmt.py` — список поясов, проверка, перевод любых встречающихся форм
  времени (datetime с поясом и без, секунды эпохи, строки ISO от обёртки аудита).
- Места вывода найдены обходом, а не по памяти: 8 `strftime` в шаблонах, время
  отчётов и отметки эталона/файрвола во «Безопасности», дата файлов в настройках,
  выгрузка диалога в markdown, 5 подписей в `app.js`, граница «сегодня» в SQL
  дашборда. Относительное время («5 минут назад» во VPN) пояса не касается.
- В «Настройках» — список: пояса России, соседи, несколько мировых, UTC. Смещение
  в подписи считается, а не вписано: у Берлина и Лондона летнее время.
- «Сегодня» на дашборде теперь с полуночи в выбранном поясе, а не с 03:00 по Москве.

**Сорвалось и исправлено по ходу.** Первая версия шла через фильтр Jinja, заведённый
в Python. Шаблоны живые, Python — после перезапуска: «Проекты», «Чатики» и
«Настройки» несколько минут отдавали 500. Поймано своей проверкой живой службы,
шаблоны сразу возвращены, все страницы — 200. Переделано на макрос
`time-macros.html`, который до перезапуска откатывается на прежний вывод; живая
служба со старым Python и новыми шаблонами проверена — все восемь страниц 200.
Вопрос, как закрыть этот разрыв насовсем, — в `current_questions.md`.

**Проверено:**
- `tests/check_timezone.py`: перевод всех форм времени (21:30 UTC → 00:30 следующих
  суток по Москве), отказ от поясов не из списка и мусора, летнее время, граница
  «сегодня» на поясе Владивостока; настройка возвращается ровно как была. Прогон на
  трёх дефектах — «сегодня» с полуночи UTC, перевод не делается, пояс не проверяется —
  падает на каждом;
- сквозная проверка в Chromium на втором экземпляре с новым кодом (рабочую службу не
  трогал; браузер нарочно в UTC, чтобы совпадение не пришло от его собственного пояса):
  выбор Владивостока **нажатием «Сохранить»**, запись в базу, время сообщения, время
  файла из скрипта, выгрузка с «(UTC+10)», отметка дашборда по Владивостоку, возврат
  Москвы той же формой — с проверкой по базе. Прогон на дефектах: скрипт не передаёт
  пояс — падает; шаблон не переводит — падает;
- оснастка трижды ошибалась сама, и каждый раз это установлено до правки продукта
  (правило 13): удвоенный префикс `/Claude` без nginx; перенаправление после отправки
  формы мимо перехвата; гонка — отправка формы долетала после «возврата» настройки,
  а проверка «Москва выбрана» ничего не доказывала, потому что Москва и так по
  умолчанию. Исправлено ожиданием ответа на отправку и сверкой с базой;
- экземпляр погашен по PID, порт молчит; все десять проверок `tests/` проходят.

**Ждёт:** перезапуска службы (кнопка в «Настройках») — до него время идёт по-старому.

## 12.09.2026 — Правая колонка проекта: шире и с вкладками «Загрузка» / «Файлы проекта»

**Статус:** выполнено
**Коммиты:** `4324c90`

**Суть задачи:** раздел «Файлы» в проектах в полтора раза шире (блок «Ход работы»
расширяется вместе с ним). Вверху две вкладки: «Загрузка» — то, что сейчас в разделе
«Файлы», и «Файлы проекта» — туда переезжает список папки выдачи из окошка. Кнопку
«Файлы проекта» из верхней панели убрать.

**Ход:**
- Колонка 250 → 375 px; «Ход работы» живёт в той же колонке и расширился с ней.
- Вкладки по образцу WAI-ARIA: щелчок, стрелки, Home/End, в строку вкладок Tab
  попадает один раз. Вид — как у закладок формы 1С. Своего механизма вкладок в
  проекте не было, сделан общий `initTabs` — годится и для других страниц.
- «Файлы проекта» стали списком, а не таблицей: в колонке 375 px четыре столбца
  не помещаются. Имя — ссылка на скачивание, ниже размер и время, как у загруженных
  файлов во вкладке рядом. Фильтр и «Обновить» сохранены.
- Список забирается при первом показе вкладки, а не при открытии страницы; после
  окончания ответа обновляется сам, если его уже смотрели, — в папке выдачи могло
  появиться сделанное. Уже показанный список на время запроса не гасится.
- Выбранная вкладка помнится: загрузка файла перезагружает страницу, и без этого
  вкладка сбрасывалась бы на первую.
- Окошко (`<dialog>`) и кнопка в верхней панели убраны; стили `.modal` остались —
  ими пользуется окно QR-кода во вкладке VPN.
- Python не менялся: всё подхватилось без перезапуска службы.

**Проверено в настоящем браузере** — headless Chromium через Playwright, на рабочей
службе через nginx. Для этого поставлены недостающие библиотеки (`libatk`, `libgbm`
и др.) через обёртку `webui-apt-install`. Проверка жмёт и смотрит:
- кнопки и окошка нет; колонка ровно 375 px, «Ход работы» той же ширины;
- по умолчанию «Загрузка»; щелчок по «Файлы проекта» переключает выбор и видимость
  (видимость — настоящая, `is_visible`, а не атрибут); список забран, ссылка ведёт
  в папку выдачи и скачивание отдаёт 200; фильтр без совпадений говорит об этом,
  очищенный — возвращает список;
- стрелка влево и End переключают, фокус едет вместе с выбором;
- после перезагрузки открыта та же вкладка; ошибок в консоли нет;
- снимки обеих вкладок просмотрены глазами.

Прогон на дефектах: снято правило `.tabpanel[hidden]` — панель файлов видна при
`hidden`, проверка падает; вкладка не забирает список — падает. Первый запуск
упал на оснастке, не на продукте (правило 13): ожидание «сеть затихла» на этой
странице не наступает никогда — там живут опрос кружков и поток ответа.
Все девять проверок `tests/` проходят.

## 12.09.2026 — Вкладка «Безопасность»: разбор инструкции и план

**Статус:** вехи 1 и 2 выполнены, дальше ждёт решений и двух шагов с паролем
**Коммиты:** `25a73f3`, `e6fb63c`, `0a03345`, `c17ce51`, `e7b5f27`

**Суть задачи:** разобрать загруженную инструкцию по ночному аудиту
безопасности сервера и продумать, как после вкладки «VPN» добавить вкладку
«Безопасность» — управление системой, чтение отчётов, настройки.

**Ход:**
- Инструкция разобрана целиком, сверена с тем, что уже стоит на машине.
  План — `bezopasnost/plan-bezopasnost.md`, копия для скачивания в
  `bezopasnost/downloads/`.
- Сперва план был положен в `exchange/` — и не появился в «Файлах проекта»:
  кнопка показывает папку `downloads/` в каталоге проекта, а `exchange/` вообще
  вне версий и к проекту не относится. Отсюда новое правило 15 в `CLAUDE.md`:
  сделанное по проекту кладётся в каталог проекта плюс копия в `downloads/`.
- Устройство предложено по образцу VPN: код аудита под root в `/opt/secaudit`,
  состояние под пользователем в `/var/lib/secaudit`, между панелью и root —
  одна обёртка `webui-sec` с закрытым набором команд и без путей в доводах.
- Найдены три места, где инструкция «как есть» сломает работающее: файрвол
  не открывает UDP (отрежет WireGuard молча), не пропускает трафик с `wg0`
  (панель по туннелю перестанет открываться), а единый каталог `/opt/secaudit`
  с владельцем-учёткой аудита при нашей модели равносилен беспарольному root.
- Вопросы записаны в `current_questions.md`.
- **Веха 0 частично снята** (`bezopasnost/sostoyanie-do.md`) — и сразу дала три
  находки, которые аудит поймал бы в первую же ночь:
  1. `unattended-upgrades` установлен, а в `20auto-upgrades` нули, журнал пуст,
     ядро стартовое для 24.04 — машина не обновлялась с установки. Ожидает 354
     обновления, из них **197 security**;
  2. **вход по SSH паролем разрешён** — проверено делом, а не чтением конфига:
     `sshd -T` без root не читается, `50-cloud-init.conf` закрыт, поэтому
     спрошен сам сервер — на попытку входа он объявляет `publickey,password`;
  3. `fail2ban` и `iptables-persistent` не установлены вовсе.
- Значения для `config.sh` взяты по факту, как требует §5: `PUB_TCP="80,443"`,
  `PUB_UDP="51820"`, `SSH_PORT=22`. Заодно подтвердилась ловушка §10 —
  `ss` печатает 22-й порт как `*:22`, и фильтр по `0.0.0.0` его не увидит.
- Вопрос про режим входа закрыт и из `current_questions.md` убран; вместо него
  три решения, которых ждёт работа: когда ставить обновления, чем закрывать
  парольный вход, ставить ли `fail2ban`.
- **Веха 1 — скрипты** (`c17ce51`). Девять правок против инструкции, все
  перечислены в `bezopasnost/README.md`. Главные: открывается UDP и принимается
  `wg0` (иначе первое применение правил отрезало бы WireGuard молча — ловушка
  из §10 самой инструкции); `remediate` не принимает путь к снимку, а делает
  свежий; файрвол не применяется автоматически, пока не применён руками хотя бы
  раз — иначе первый же ночной прогон включил бы default-deny без проверки
  доступа из второго окна.
- **Веха 2 — обёртка** `infra/webui-sec.sh` (`e7b5f27`), строка в
  `sudo-allowed.list` (стало 22 команды, `visudo` проходит), раскладка кода
  в `/opt/secaudit` и состояния в `/var/lib/secaudit` через
  `install-root-helpers.sh`.

**Проверки — и что они показали.**
- `tests/check-diff.sh`: 10 утверждений. Прогнана на трёх дефектах, на каждом
  падает (3, 2 и 2 провала). Два дефекта нашёл сам прогон: `comm` получал
  числовую сортировку и врал результатом, а при политике ACCEPT сверка
  объявляла отрезанными все службы разом.
- `tests/check-wrapper.sh`: 21 утверждение, четыре дефекта, на каждом падает.
- **Дважды подряд подвела оснастка, а не код.** Сперва `sed` не внёс дефект
  «только TCP» — не учёл отступ, и проверка осталась зелёной впустую. Затем
  стенд обёртки спрашивал «отказано ли», а не «почему»: ослабленная проверка
  хэша прошла насквозь, потому что отказ приходил ниже по течению и по другому
  поводу. Оба раза зелёный результат давала оснастка. Теперь дефект вносится
  и сверяется глазами, а отказ проверяется по причине.

## 12.09.2026 — Панель управления VPN во вкладках рабочего места

**Статус:** выполнено, ждёт перезапуска службы
**Коммиты:** `f88304f`

**Суть задачи:** вкладка «VPN» между «Проекты» и «Настройки». На панели —
добавление и удаление пользователей, QR-коды для подключения, контроль трафика.

**Ход:**
- Панель не ходит в `/etc/wireguard` и не зовёт `wg`: всё через ту же обёртку
  `webui-vpn`. Для трафика добавлена команда `peers` — скрипт `list-peers.sh`
  сшивает имена из конфига сервера с показаниями `wg show ... dump` по открытому
  ключу. Числами, а не «1,05 МиБ» и «2 minutes ago»: разбирать человеческий вид
  на стороне панели — значит зависеть от языка и округления.
- Трафик и время последней связи обновляются опросом раз в 10 с. Готовые строки
  считает Python и отдаёт их же в JSON: повтори форматирование в браузере —
  и «5 минут назад» разошлось бы с тем, что нарисовано при отрисовке страницы.
- QR-код рисуется по требованию, в разметку всех клиентов сразу не кладётся:
  в нём закрытый ключ. Кэширование на маршрутах с ключами выключено, имя
  сверяется с образцом до обращения к диску — иначе `?name=../..` отдал бы
  через панель `webui.env` с `SECRET_KEY`.
- Вкладка в шапке собрана вручную (`{{ root_path }}/vpn`), а не через `url_for`:
  шапка общая, и `url_for` на ещё не заведённый маршрут уронил бы в 500 не одну
  страницу, а все сразу. Те же грабли ловились на кружках состояния тем.

**Проверки — и что они показали.**
- `tests/check_vpn_panel.py`: 40 утверждений, второй экземпляр на свободном
  порту (рабочую службу не трогал), своя сессия в базе, за собой убирает.
- Первый прогон прошёл зелёным **вхолостую**: установленная копия обёртки ещё
  старая, клиентов панель не получила, и проверки трафика сравнивали пустое
  с пустым. Добавлена заглушка обёртки с вымышленными клиентами — теперь
  отрисовка и опрос проверяются на непустых данных, а отдельная проверка
  говорит вслух, что на живом сервере трафик не проверен.
- Прогон на четырёх дефектах, на каждом падает: образец имени без привязки
  к началу и концу (проходят `a b`, `a/b`, имя в 33 знака); строки сводки
  разбираются как попало; итог трафика берётся от первого клиента; опрос
  отдаёт сырые числа вместо готовых строк.
- `tests/check-clients.sh` поймал ошибку на единицу в `list-peers.sh`:
  `«# client: »` — десять знаков, а имя резалось с одиннадцатого, и все имена
  теряли первую букву.
- Все восемь проверок `webui/tests/` проходят, `app.js` разбирается, шаблоны
  разбираются, `shellcheck` чист.

**Ждёт двух шагов с вашей стороны:**
1. «Настройки» → «Перезапустить приложение» — до перезапуска маршрута `/vpn`
   нет, вкладка ведёт в 404. Сам перезапустить не могу: команды выполняются
   внутри `webui.service`, и перезапуск оборвал бы эту сессию.
2. `sudo bash ~/main/infra/install-root-helpers.sh` — обновить копию обёртки,
   иначе таблица клиентов будет пустой с подсказкой вместо трафика.

## 11.09.2026 — VPN-сервер на своей машине

**Статус:** выполнено — сервер работает, ждёт проверки с клиента
**Коммиты:** `7194304`, `cbb6bd0`

**Суть задачи:** поднять на сервере VPN, настроить доступ, чтобы можно было
работать через него.

**Ход:**
- Выбран WireGuard: в ядре, конфиг в десяток строк, официальные клиенты под все
  системы. OpenVPN при 2.9 ГБ RAM дороже и требует своего центра сертификации.
- Каталог `vpn-server/`: `install-wireguard.sh` (пакеты, ключи, конфиг, транзит,
  брандмауэр, автозапуск), `add-client.sh` (пир + файл + QR-код), `remove-client.sh`
  (отзыв без перезапуска службы), `README.md`.
- Установщик повторно запускается без последствий: ключи и конфиг с клиентами
  не перезаписываются, к работающей службе настройки подтягиваются через
  `wg syncconf` — сессии остальных клиентов не рвутся.
- `ufw` скрипт только дополняет и **не включает**: включение с настройками по
  умолчанию отрезало бы SSH, а сервер удалённый.
- Правила `iptables` вставляются в начало цепочек (`-I FORWARD 1`), а не в конец:
  при включённом `ufw` его переход стоит первым, и дописанное в конец правило
  не сработало бы — туннель поднялся бы, а трафик не пошёл.
- Закрытые ключи в git не попадают: пишутся в `/etc/wireguard`, копии для переноса —
  в неверсионируемый `exchange/vpn/`.

**12.09.2026, доступ к командной строке появился** — поднят флаг «разрешить
запуск команд». Сделано всё, что доступно без пароля root:
- поставлены `wireguard`, `qrencode`, `shellcheck`, `fakeroot` (беспарольная
  обёртка `webui-apt-install`); `shellcheck` на всех скриптах чист;
- `tests/check-clients.sh`: выдача и отзыв прогоняются на поддельном
  `/etc/wireguard`. Подделать root удалось не сразу — `unshare -r` запрещён
  ядром (Ubuntu 24.04 режет непривилегированные namespace), взял `fakeroot`.
  Настоящими оставлены вызовы `wg`, считающие ключи: подмени их заглушкой —
  проверка перестала бы что-либо значить;
- разведка: `eth0`, адрес `46.8.178.196` прямо на интерфейсе (NAT провайдера
  нет), порт свободен, `ufw` выключен, модуль `wireguard.ko.zst` на месте.
  Значит установщик определит всё сам.

**Проверено:** девять утверждений `tests/check-clients.sh` проходят; проверка
прогнана на трёх дефектах и на каждом упала — адрес выдаётся без учёта занятых,
отзыв вычищает конфиг до конца файла, занятое имя не проверяется. Установка
как таковая не выполнялась: пять проверок «на живом» из README не пройдены.

**12.09.2026, беспарольный доступ на будущее** (`cbb6bd0`). По вашему решению
заведена обёртка `infra/webui-vpn.sh` → `/usr/local/sbin/webui-vpn`: закрытый
набор `install | add | remove | list | status`, произвольные доводы не проходят.
- Ключевое: обёртка запускает **не** скрипты из `vpn-server/`. Тот каталог
  доступен на запись агенту, и строка в `sudo-allowed.list` вида «bash
  vpn-server/*» была бы просто беспарольным root — достаточно переписать
  скрипт. Работает root-овая копия в `/usr/local/lib/webui-vpn`, её владельца
  и права обёртка проверяет перед каждым запуском.
- Следствие, которое легко забыть: правка скриптов в репозитории теперь
  вступает в силу только после повторного `install-root-helpers.sh`.

**Проверка уличила сама себя — и нашла дыру.** `tests/check-wrapper.sh` в первом
варианте считал отказом любой ненулевой код возврата и прошёл зелёным на двух
внесённых дырах: обёртка пропускала негодный довод внутрь, спотыкалась там об
отсутствующую копию и жаловалась, а проверка засчитывала это за отказ. Переписан
на дословную сверку разобранной команды (`WEBUI_VPN_CHECK=1` показывает её, не
запуская) — и сразу нашёл настоящую дыру: `-` входил в разрешённые знаки имени,
поэтому `--help` проходил как имя клиента. Исправлено в обёртке и в
`add-client.sh`: первым знаком обязана быть буква или цифра.

**Проверено:** `check-wrapper.sh` — 21 утверждение, падает на четырёх дырах
(имя не проверяется, `install` берёт любые доводы, каталог выдачи не передаётся,
проверки root нет); `check-clients.sh` — девять утверждений, падает на трёх
дефектах; `shellcheck` на всех скриптах чист.

**12.09.2026, сервер поднят.** Разовый шаг с паролем выполнен, дальше всё сделано
беспарольно через `webui-vpn`: `install` → служба активна и в автозапуске,
`46.8.178.196:51820/udp`, туннель `10.8.0.1`; заведены `notebook` (`10.8.0.2`)
и `phone` (`10.8.0.3`), конфиги в `exchange/vpn/`, права `600`, каталог из git
исключён (`git check-ignore` подтверждает).

**Проверено на живом:** служба `active`, автозапуск `enabled`, `51820/udp`
слушается на v4 и v6, `/proc/sys/net/ipv4/ip_forward` = 1, интерфейс `wg0`
с адресом `10.8.0.1/24`, маршрут в сеть туннеля есть, оба пира видны в
`webui-vpn status`. Применение правил транзита и NAT подтверждено записями
`wg-quick` в журнале (`iptables -I FORWARD 1 …; iptables -t nat -I POSTROUTING 1 …`).

**Чего проверить изнутри нельзя** и что остаётся за клиентом: доходит ли UDP
через брандмауэр хостера и идёт ли трафик через туннель. Показывает первое
подключение: адрес на «мой IP» должен стать `46.8.178.196`.

Заодно поправлена подсказка в конце установщика: запущенный через обёртку, он
показывал путь к служебной копии в `/usr/local/lib` как команду. Правка
вступит в силу при следующем `install-root-helpers.sh` — работает копия.

## 11.09.2026 — Кружок состояния рядом с темой проекта

**Статус:** выполнено, цвета появятся после перезапуска
**Коммиты:** `4093849`

**Суть задачи:** в перечне тем проекта рядом с каждой темой — кружок: зелёный,
если всё выполнено, красный, если есть что в работе.

**Ход:**
- Красный в двух случаях, и оба значат «не доведено»: по теме прямо сейчас идёт
  ответ (виден по запуску в памяти) **или** последняя запись — вопрос без ответа
  либо ошибка. Второй случай важнее: так выглядит тема, чей ответ оборвался
  перезапуском или сбоем, и из перечня это раньше было не разглядеть.
- Зелёный — последняя запись в теме ответ движка. Пустая тема серая, не зелёная:
  зелёный значит «сделано», а в ней ничего не делали.
- Цвет не единственный признак: то же самое сказано словами в подсказке и для
  чтения с экрана — иначе для тех, кто не различает красный и зелёный, признака нет.
- Состояние обновляется само: опрос раз в 10 с (соседняя тема может считаться
  фоном), а для открытой темы кружок перекрашивается сразу по началу и концу
  ответа — там об этом известно раньше опроса.
- Страница и опрос ходят через одну функцию `_topics_state_map`: разойдись они,
  кружок менял бы цвет сам по себе при перезагрузке.

**Грабли, пойманные на себе.** Шаблон подхватывается сразу, а маршрут — это Python,
он живёт только до перезапуска. `url_for` на ещё не заведённый маршрут уронил всю
страницу «Проекты» в 500 — поймано проверкой живой страницы через минуту после
правки. Поэтому адрес опроса собирается вручную из уже известного, а сам кружок
не рисуется, пока состояние со страницей не приходит. До перезапуска страница
выглядит ровно как прежде, после — с кружками.

**Проверено:**
- `tests/check_topic_state.py` заводит свой проект с темами и убирает за собой;
  прогон на трёх дефектах, которые он обязан ловить: любая непустая тема зелёная —
  падает; идущий ответ не учитывается — падает; берётся первая запись вместо
  последней — падает;
- новый код поднят вторым экземпляром на свободном порту (рабочую службу не трогал):
  страница отдаёт кружки, опрос отдаёт те же состояния, расхождений нет;
  экземпляр погашен по PID, рабочая служба отвечает;
- живая страница на рабочей службе — 200, вложенность тегов сбалансирована;
- `app.js` разбирается без ошибок, все семь проверок каталога `tests/` проходят.

**Ждёт:** перезапуска службы — до него кружков не будет, страница работает как прежде.

## 11.09.2026 — Кнопка «Файлы проекта» со скачиванием

**Статус:** выполнено
**Коммиты:** `eec023e`

**Суть задачи:** файлы, которые Claude создаёт в рабочем каталоге проекта, нечем
забрать из браузера. Нужна кнопка в командной панели проекта: открывает отдельное
окошко (не окно браузера) со списком файлов каталога, каждый можно скачать.

**Ход:**
- Кнопка «Файлы проекта» в командной панели открывает окошко внутри страницы —
  не окно браузера, как и просили.
- Показывается **папка выдачи** `downloads/` в каталоге проекта, а не весь рабочий
  каталог: там сотни файлов кода, и нужный среди них не найти — а открывают окошко
  ровно за этим. Папка заводится при создании проекта и при первом обращении:
  проекты, заведённые записью в таблице, форму создания не проходили, и папки у них
  нет. Сама папка не версионируется — строка в `.gitignore`.
- Список свежими сверху, с размером и временем; предел 2000 файлов и глубина 12,
  при обрезке окошко об этом говорит прямо.
- Закрытое не показывается и не отдаётся: `.env*`, ключи (`.key`, `.pem`), `.git`,
  `.venv`, `node_modules`, `__pycache__`, сборочные каталоги. Символьные ссылки
  пропускаются: ссылка наружу всё равно была бы отбита, а строка в списке обещала бы
  файл, которого не дадут.
- Путь из запроса проверяется после `resolve()`, и один и тот же заслон отбивает
  `..`, абсолютный путь и ссылку наружу. Без него `?path=../../.config/webui/webui.env`
  отдавал бы кнопкой «скачать» `SECRET_KEY` и пароль базы.

**Проверено:**
- `tests/check_project_tree.py`: показана только папка выдачи, порядок по свежести,
  выход из неё отбит;
- прогон на дефекте, который проверка обязана ловить: заслон пути снят — проверка
  падает с кодом 1 и называет пропущенный путь (`../main.py`); на исправном коде 0.
  Код возврата сверен отдельно: «ПРОВАЛ» в выводе и нулевой код возврата вместе
  выглядели бы как успех;
- все шесть проверок каталога `tests/` проходят;
- работа страницы подтверждена в браузере пользователем.

## 11.09.2026 — Свод для повторения интерфейса: `webclaude.md`

**Статус:** выполнено
**Коммиты:** `61b2844`

**Суть задачи:** собрать в один файл `webclaude.md` всё, что нужно для создания
аналогичного веб-интерфейса: стек, структура проекта, схема базы, устройство
драйвера, правила работы, развёртывание, грабли.

**Ход:**
- Материал собран из `architect.md`, `CLAUDE.md`, `tech_debt.md`, `result.md` и
  самого кода: комментарии в `claude_driver.py`, `runs.py`, `transcripts.py`,
  `usage.py`, `restart.py` и в конфигах `deploy/` хранят разбор случаев, которых
  в документации нет.
- Семнадцать разделов: решение и предпосылки, стек, структура, адреса, база,
  драйвер, фоновые запуски, права, секреты, перезапуск, учёт расхода,
  дизайн-система, развёртывание, правила работы, порядок сборки по вехам,
  грабли одним списком.
- Файл положен в `webui/`, а не в корень: правило 10 оставляет в корне только
  файлы сопровождения.

**Проверено:** числа, флаги и куски конфигов сверены с исходниками, а не взяты
по памяти; служба `active`, `/Claude/healthz` отвечает `ok`.

## 11.09.2026 — «Новая тема» в начало панели проекта

**Статус:** выполнено
**Коммиты:** `96d004e`

**Суть задачи:** перенести поле «Новая тема» и кнопку «Добавить тему» налево, чтобы
название проекта и флажок «разрешить запуск команд» шли после них.

**Ход:**
- Порядок в панели: форма новой темы → разделитель → название и каталог проекта →
  разделитель → флажок запуска команд. Распорку (`spacer`) перенёс в конец, иначе она
  расталкивала бы части панели по краям.

**Проверено:** страница забрана у uvicorn с временной сессией, порядок частей считан
с отрисованной разметки; на прежнем шаблоне проверка падает. Вложенность тегов
сбалансирована.

## 11.09.2026 — Уход с вкладки обрывает работу

**Статус:** выполнено, живая проверка за перезапуском
**Коммиты:** `1e705c3`, `22feb15`

**Суть задачи:** если во время ответа перейти на другую вкладку интерфейса
(например, «Дашборд»), работа обрывается и ответ теряется.

**Причина (найдена в коде, `projects.py:259` и `chats.py`):** ответ движка идёт
не сам по себе, а внутри тела HTTP-ответа — `StreamingResponse` с генератором,
который тут же и запускает `claude_driver.run`. Переход на другую вкладку —
это обычная навигация браузера: соединение закрывается, Starlette отменяет
генератор, драйвер получает отмену и гасит процесс `claude`. Ответ при этом
не сохраняется: запись в базу делается по событию `result`, а до него дело
не доходит.

**Что сделано.** Запуск переехал в задачу приложения — `services/runs.py`.
События копятся списком, HTTP-поток стал только читателем: его можно оборвать
и открыть заново с нужной позиции. Отправка и показ разделены: `POST send`
ставит работу и сразу отвечает, события берутся с `GET stream`.

- Список, а не очередь: вернувшаяся страница получает ответ целиком. Очередь
  отдала бы обрывок с того места, до которого дошло дело, и выглядело бы это
  как потерянное начало ответа.
- Законченный запуск держится полчаса, но при открытии страницы не
  проигрывается заново — для этого признак `active`. Без него прошлый ответ
  лёг бы вторым экземпляром поверх того же ответа, уже показанного из базы.
- Проверка «ответ уже идёт» стоит до записи вопроса в базу: иначе при отказе
  вопрос остался бы висеть в ленте без ответа.
- Страница сама переподключается, если соединение моргнуло, — с той позиции,
  на которой остановилась.

**Проверено:**
- `tests/check_runs_detach.py`: читатель уходит после двух событий — работа
  доходит до конца, движок выдаёт всё; вернувшийся читатель получает ответ
  целиком; продолжение с позиции отдаёт ровно хвост;
- прогон на дефектах, которые стенд обязан ловить: очередь вместо списка —
  вернувшийся получает 0 событий вместо 6, падает; снятый признак `active` —
  прошлый ответ проигрывается заново, падает;
- модули загружаются, оба новых пути (`chat_stream`, `topic_stream`)
  зарегистрированы, шаблоны и `app.js` разбираются без ошибок.

**Не проверено:** живое поведение в браузере. Новый код подхватится только
после перезапуска службы, а перезапуск гасит и тот ответ, из которого его
запускают, — поэтому он поставлен в очередь и сработает по завершении. Проверка
на живой странице — следующим шагом.

## 11.09.2026 — Дашборд под тарифный план, а не под доллары

**Статус:** выполнено
**Коммиты:** `c5b76a4`

**Суть задачи:** на дашборде показан расход в долларах, что для подписки Pro
бессмысленно — деньги за запросы не списываются. Нужен расход в токенах и
состояние тех окон, которыми план и ограничен: пятичасового и недельного —
сколько израсходовано, сколько осталось, когда окно обнулится.

**Ход:**
- Доллары убраны с видного места: `total_cost_usd` от CLI — это пересчёт по
  тарифам API, «сколько стоило бы то же самое без подписки». Осталось
  справочной строкой; из строки под ответом в ленте убрано тоже.
- Два скользящих окна: набрано токенов, доля предела, остаток, когда обнулится.
  Окно начинается с первого запроса, а не в круглый час.
- Предел задаётся в настройках и по умолчанию не задан. Числом Anthropic
  пределы не публикует; проверено по документации: Admin и Analytics API личным
  подпискам не выдаются («The Admin API is unavailable for individual accounts»)
  и показывают прошлый расход, а не остаток, у телеметрии OpenTelemetry метрики
  остатка нет ни одной. Выдуманный предел показывал бы уверенный остаток,
  которого никто не обещал.
- Считается по стенограммам сессий (`~/.claude/projects/*/*.jsonl`), а не только
  по своей базе: запуски `claude` из терминала расходуют то же окно. База
  осталась запасным источником, и страница говорит, какой сработал.

**Главная ловушка — двойной счёт.** Один ответ движка разложен в стенограмме
на несколько строк, и в каждой лежит один и тот же блок `usage` — полный расход
ответа, а не доля. Построчное сложение давало 1 229 685 выходных токенов вместо
настоящих 554 316, и выглядело бы это совершенно правдоподобно. Строки сводятся
по ключу «запрос + сообщение»; `uuid` не годится — он у каждой строки свой.

**Проверено:**
- прогон на дефектах: построчный счёт — падает; чтение недописанной строки
  (стенограмму дописывают прямо во время чтения) — падает; подмена начала окна
  на «пять часов назад» — падает;
- первая попытка стенда дефект с недописанной строкой **не поймала** —
  дописан случай, где строка дочитывается до конца;
- числа сверены независимым счётом по тем же файлам;
- живая страница после перезапуска: источник «стенограммы», 156 ответов
  за пять часов; при заданном пределе рисуется мера и остаток. Пределы после
  проверки возвращены в ноль — числа выбирать пользователю.

## 11.09.2026 — Установка пакетов без пароля

**Статус:** выполнено
**Коммиты:** `d729040`

**Суть задачи:** разрешить агенту ставить пакеты беспарольно, чтобы установка
`certbot` и подобное не упиралась в ручной шаг.

**Ход:**
- Строка `/usr/bin/apt-get install *` в список **не** добавлена, и вот почему:
  у apt-get есть параметр `-o ...::Pre-Invoke`, выполняющий произвольную команду
  от root, а доводом можно дать путь к локальному `.deb`, чьи сценарии тоже идут
  от root. Такая запись была бы беспарольным root, и остальной список после неё
  не значил бы ничего.
- Вместо неё — обёртка `infra/webui-apt-install.sh` (ставится как
  `/usr/local/sbin/webui-apt-install`): пропускает только имена пакетов, всё
  остальное отклоняет. Тот же приём, что уже применён к `webui-deploy`.
- Заодно разрешён `/usr/local/sbin/webui-tls-issue` — копия `tls-letsencrypt.sh`:
  выпуск сертификата пишет в `/etc/letsencrypt`, одной установкой пакетов туда
  не добраться, а без этого задача с сертификатом всё равно упиралась бы в пароль.
- `infra/install-root-helpers.sh` — установка всех трёх обёрток одной командой.
  Обёртка обязана принадлежать root и быть закрытой пользователю на запись:
  иначе разрешение на неё — это разрешение на любой код от root. Отсюда же
  следует, что правка обёртки в репозитории не действует, пока её не поставили.
- Примечание в шапке `sudo-allowed.list` переписано: прежнее «установка пакетов
  сюда сознательно не включена» после этой правки стало бы неправдой.

**Проверено:**
- прогон на дефектах, которые проверка должна ловить: `-o APT::...::Pre-Invoke`
  отклонён, `./evil.deb` отклонён, `evil.deb` без слэша отклонён — последний
  случай проходил бы по одному образцу имени, его ловит отдельная проверка на
  `.deb` (apt-get считает файлом довод и со слэшем, и просто с этим окончанием);
  годное имя `certbot` проходит; пустой список доводов даёт подсказку;
- `apply-sudo.sh --dry-run` на стенде: 20 команд root, `visudo -c` доволен,
  обе новые строки на месте.

- **11.09.2026, применено** пользователем: обе обёртки стоят в `/usr/local/sbin`
  владельцем root с правами 755, все три разрешения видны в `sudo -n -l`.
  Первое же дело новой обёртки — установка `certbot 2.9.0` — прошло без пароля.

## 11.09.2026 — Ключи под root: файл и врезка в юнит

**Статус:** выполнено, применение за вами
**Коммиты:** `cae66e4` — ключи под root

**Суть задачи:** подготовить перенос ключей в файл с владельцем root — остаток
по пункту 2 техдолга: при поднятом `allow_bash` тема читает файл пользователя
командой, и запрет по пути тут не помогает.

**Ход:**
- `infra/secrets-to-root.sh`: копия под root в `/etc/webui/webui.env` (600),
  врезка в юнит, перезапуск, проверка ответом приложения, и только потом —
  уборка копии пользователя в `/etc/webui/webui.env.bak`, тоже под root.
  Ничего не удаляется, откат печатается в конце.
- Врезка версионируется: `webui/deploy/webui.service.d/secrets.conf`.
- **Правка до переноса, без которой он сделал бы хуже:** список вычищаемых из
  окружения ключей брался из файла настроек. После переноса файла у пользователя
  не будет — набор опустел бы, и `claude` снова унаследовал бы `SECRET_KEY`
  и пароль базы. Список задан явно (`config.APP_ENV_KEYS`), имена из файла
  добавляются сверх него.
- В беспарольный sudo скрипт не добавляется: врезка исполняется от root.

**Проверено:**
- стенд на будущем состоянии (файла нет, значения от systemd): ключи потомку
  не достаются, `PATH` и `HOME` на месте;
- тот же стенд на прежнем поведении (набор из файла) ключи находит — различает;
- проверка и откат скрипта прогнаны стендом со стубами `curl` и `systemctl`:
  при 200 врезка остаётся, при 502 снимается, служба возвращается, код возврата 1;
- адрес проверки сверен делом: `http://127.0.0.1:8000/healthz` отвечает 200
  напрямую, без префикса `/Claude` — иначе скрипт откатывался бы на исправной
  службе;
- служба перезапущена с новым списком ключей, `healthz` 200, вход открывается.

**Стенд поймал мою же порчу текста:** массовая замена имени переменной залезла
внутрь русских слов, и в выводе появилось «приложение CODEило 200». Исправлено.

**Не проверено:** сам перенос — он требует root с паролем. Скрипт рассчитан на
то, что первый запуск будет вашим: при неудаче он снимает врезку и возвращает
службу сам.

## 11.09.2026 — Ключи вынесены из каталога проекта

**Статус:** выполнено
**Коммиты:** `4076d48` — ключи вне каталога проекта

**Суть задачи:** вынести `.env` за пределы рабочего каталога проекта — пункт 5
техдолга: `SECRET_KEY` и пароль базы читались из темы проекта.

**Ход:**
- Файл перенесён в `~/.config/webui/webui.env` (права 600, каталог 700).
  Путь задаётся переменной `WEBUI_ENV`, по умолчанию — этот.
- `config.py` читает файл оттуда и внятно падает, если настроек нет.
- Выяснилось, что переноса мало: `--allowed-tools Read` авто-одобряет **любой**
  путь. Добавлен явный запрет `Read`/`Write`/`Edit` по пути.
- Найдена вторая дыра: dotenv кладёт значения в `os.environ`, и запускаемый
  `claude` наследовал `SECRET_KEY` с паролем базы прямо в окружении. Драйвер
  теперь запускает его с вычищенным окружением (`_child_env`).
- `deploy/README.md` и `.env.example` — под новый порядок установки.

**Проверено:**
- дефект показан до правки: тема проекта прочитала `.env` инструментом `Read`
  и перечислила имена ключей;
- после переноса тот же пробник показал, что файл читается и по новому пути —
  поэтому правка не остановилась на переносе;
- синтаксис запрета подобран пробником на четырёх вариантах. Работают
  `Read(//путь)` и `Read(//каталог/**)`; вариант с одним слешем молча
  не действует — файл при нём читался. Это записано в `architect.md`;
- приёмка на настоящих параметрах проекта: чтение — ОТКАЗАНО, ключей в
  окружении нет;
- вычистка окружения проверена детерминированно, без модели: по словарю и по
  выводу `env` в дочернем процессе; на прежнем поведении стенд ключи находит,
  то есть различает;
- служба перезапущена, `healthz` 200, вход открывается.

**Ненадёжный свидетель.** Первый приёмочный прогон сам сообщил «BASH: ОТКАЗАНО».
Проверка отдельными командами показала обратное: `wc -c` по файлу отработал,
и обход через `..` тоже. Модель описала желаемое, а не случившееся — красный
и зелёный ответ движка одинаково требуют проверки делом.

**Что осталось.** При поднятом `allow_bash` файл читается командой где угодно.
Дописано в пункт 2 техдолга вместе со способом закрыть именно ключи —
`EnvironmentFile` юнита с владельцем root.

**Техдолг:** пункт 5 закрыт.

## 10.09.2026 — Блок «Ход работы»: промежуточные действия из ленты в свою панель

**Статус:** выполнено
**Коммиты:** `1f0de48`

**Суть задачи:** промежуточные события (`инструмент: Bash`) засоряют ленту сообщений.
Вынести их в отдельный блок справа внизу, под «Файлами», с информационной строкой,
по которой видно, что процесс идёт: таймер, счётчик шагов, расход токенов.

**Ход:**
- Правая колонка в «Проектах» разделена надвое: «Файлы» и «Ход работы», каждый блок
  прокручивается сам. События инструмента идут в нижний блок; в ленте остаётся разговор.
- Информационная строка: во время ответа «● идёт 1:23 · шагов 7 · ↑ 1 240 токенов»,
  после — время, стоимость, итоговые токены. Прогресс-бар не делался намеренно:
  у агентской работы нет известного конца, шкала до 100% врала бы.
- Живой расход берётся из `stream_event` (`message_start` / `message_delta`). В пределах
  шага счёт накопительный, на новом шаге начинается заново — поэтому начало шага
  помечается признаком `new_message`, иначе счётчик откатывался бы назад.
- В чатиках панели нет (инструменты выключены), поэтому время идёт в самом пузыре
  ответа: «думает 1:23».

**Проверено:**
- `tests/check_usage_events.py` — расход доходит, два шага считаются как 155 токенов;
  на коде без правки драйвера проверка падает;
- страница «Проекты» забрана у uvicorn с временной сессией: код 200, разметка блока
  на месте, вложенность тегов сбалансирована (разбор `html.parser`);
- `app.js` разбирается без синтаксических ошибок (`esprima`, node в системе нет);
- свежесть отдачи: в `/static/js/app.js` есть `createRunPanel`, в CSS — правила блока.

**Ждёт:** живой счётчик токенов — часть драйвера, он подхватится только после
перезапуска службы (кнопка в «Настройках»). Таймер, шаги и сам блок работают сразу.

## 10.09.2026 — «Сбой обработки: Separator is not found, and chunk exceed the limit»

**Статус:** выполнено
**Коммиты:** `267c673`

**Суть задачи:** работа над полосой прокрутки в поле сообщений второй раз обрывается
ошибкой «Сбой обработки: Separator is not found, and chunk exceed the limit».
Найти причину и убрать её.

**Ход:**
- К вёрстке отношения не имело. Вывод движка читался через `StreamReader.readline()`,
  у которого буфер 64 КиБ; движок укладывает в одну строку JSON целый файл, вывод
  команды или текст ответа. На строке длиннее буфера летел `ValueError`, он выходил
  из генератора в обработчик (`chats.py`, `projects.py`) и показывался как «Сбой
  обработки». Правка вёрстки упиралась в это гарантированно: чтение `app.css`,
  шаблонов, `git diff` даёт крупные строки.
- Сделан свой построчный разбор без ограничения на длину строки; таймаут считается
  на молчание движка, а не на строку. Так же читается `stderr` — там была та же мина.
  На строку длиннее 64 МиБ стоит потолок: такая пропускается, поток не рвётся.

**Проверено:**
- `tests/check_long_line.py` — строка в 300 000 символов, отданная кусками, проходит
  целиком; на прежнем коде проверка падает ровно заявленной ошибкой;
- служба перезапущена в 20:54 UTC уже с исправленным драйвером.

**Попутно:** перезапуск службы из темы проекта гасит собственный процесс — шелл ушёл
с кодом 137, `/proc/self/cgroup` показывает `webui.service`. Предупреждение из
`architect.md` подтвердилось на деле; отсюда служба больше не перезапускается.
Ещё: беспарольная запись sudo дословна — `systemctl restart webui`, форма
`restart webui.service` под неё не подпадает и просит пароль.

## 10.09.2026 — Диалог: поле ввода всегда внизу, лента сообщений со своей прокруткой

**Статус:** выполнено, ждёт подтверждения глазами
**Коммиты:** `1f0de48`

**Суть задачи:** в чатиках и темах проектов поле ввода запроса должно всегда
оставаться внизу страницы, а лента сообщений — прокручиваться внутри себя,
не уводя вниз всю страницу.

**Ход:**
- Правка лежала незакоммиченной с двух прежних попыток, сорванных ошибкой чтения
  потока (см. запись про «Сбой обработки»). Разобрана и доведена: `.shell--fixed`
  на `height: 100dvh`, страницы диалога занимают ровно окно.
- Цепочка высот сомкнута сквозным `min-height: 0`: `main--flush` → `workspace` →
  `chat` → `chat__scroll`. Без этого `flex-basis: 0%` не разрешается и вниз уезжает
  вся страница вместе с полем ввода.
- Боковые списки прокручиваются сами, `overscroll-behavior: contain` не даёт
  докрученной до края ленте утянуть за собой страницу.

**Проверено:** цепочка правил прочитана в CSS, страница отдаётся с нужными классами.
Визуально в браузере ещё не подтверждено — вёрстку такого рода честно проверяет только
глаз, стенда с браузером на сервере нет.

## 10.09.2026 — Домен и сертификат Let's Encrypt

**Статус:** выполнено
**Коммиты:** `30e1b61`, `4e29bec`, `4e70256`

**Суть задачи:** привязать появившееся доменное имя к серверу и перевести TLS с
самоподписанного сертификата на Let's Encrypt — пункт 1 техдолга.

**Ход:**
- Подготовлена домен-независимая часть: в HTTP-блоке nginx заведён путь для проверки
  владения доменом (`/.well-known/acme-challenge/`), который не уводится редиректом
  на HTTPS — иначе выпуск сертификата невозможен, пока сертификата ещё нет.
- Написан `infra/tls-letsencrypt.sh` — root-шаг пользователя: установка `certbot`,
  webroot, выпуск сертификата, hook на перезагрузку nginx после продления.
- Конфиг разложен (`sudo webui-deploy`) и проверен на живом сервере. До раскладки
  путь проверки отдавал 301 на HTTPS — то есть выпуск сорвался бы; после раскладки
  отдаёт 404 (файла ещё нет, его кладёт certbot), а `/Claude/` по HTTP по-прежнему
  уходит редиректом на HTTPS.

- **10.09.2026, домен назван:** `mokeevasky.ru`. Проверено, куда он указывает сейчас —
  `178.210.92.188`, это парковка регистратора (в ответе `openresty`/`kong`, 404).
  На сервер (`46.8.178.196`) запись пока не переведена, поэтому выпуск не запускался:
  проверяющий Let's Encrypt пошёл бы по A-записи и попал бы на парковку.
- Путь проверки владения проверен уже под новым именем — запросом на сервер
  с `Host: mokeevasky.ru`: `/.well-known/acme-challenge/` отдаёт 404 (файла нет,
  его кладёт certbot), а не 301 на HTTPS. Значит после перевода A-записи проверка
  пройдёт. Обычная страница тем же Host уходит редиректом на
  `https://mokeevasky.ru/Claude/` — как и задумано.

- **11.09.2026, имя переведено на сервер.** Зона уехала с NIC.RU на Cloudflare
  (`priscilla`/`peter.ns.cloudflare.com`), оба имени — `mokeevasky.ru` и
  `www.mokeevasky.ru` — указывают на `46.8.178.196`. Проксирование Cloudflare
  выключено: отдаётся наш адрес, а не их, значит проверяющий придёт прямо к нам.
  Проверено не только по DNS: `http://mokeevasky.ru/Claude/` отвечает нашим nginx
  (`Server: nginx/1.24.0 (Ubuntu)`, редирект на HTTPS), путь проверки владения
  отдаёт 404 с адреса `46.8.178.196`.
- Вопрос про `www` снялся сам: раз имя ведёт на сервер, сертификат выпускается
  на оба — иначе адрес с `www` давал бы предупреждение о подлинности. Запись
  из `current_questions.md` убрана.
- `infra/tls-letsencrypt.sh` научен нескольким именам: почта опознаётся по собачке,
  остальные доводы — имена; A-запись и путь проверки проверяются для каждого имени
  отдельно (одно негодное сорвало бы выпуск целиком). Добавлены `--cert-name`
  (иначе при добавлении имени certbot заводит соседний каталог `домен-0001`
  и пути в nginx разъезжаются) и `--expand` (иначе повторный запуск с новым именем
  в неинтерактивном режиме упирается в «часть имён уже покрыта» и молчит).
- **Попутно найдена ошибка в самой проверке** — она же была и в однодоменной
  версии: при имени без A-записи скрипт обрывался молча, кодом 2, не дойдя до
  объяснения. Причина — `set -e` с `pipefail`: `getent` возвращает ошибку, и
  присваивание `resolved=$(...)` валит скрипт целиком. Вылечено `|| true`.

**Проверено** (стенд: копия скрипта без root-проверки, обрывается перед установкой
`certbot`, — root-шаги без пароля не прогнать):
- оба наших имени с почтой — проходят, разбор доводов верен: основное
  `mokeevasky.ru`, почта отделена, имён два;
- прогон на дефектах, которые проверка должна ловить: чужое имя (`example.com`,
  адрес Cloudflare) — отказ с указанием, какое именно имя негодно; имя без
  A-записи — отказ с объяснением; пустой список имён и один довод-почта — подсказка
  по применению. До правки `|| true` случай «нет A-записи» давал пустой вывод
  и код 2 — то есть проверка чинилась по настоящему, воспроизведённому отказу.

- **11.09.2026, выпущено.** После того как установка пакетов вошла в беспарольный
  набор (запись выше), выпуск сделан целиком без пароля: `certbot 2.9.0` поставлен
  обёрткой, сертификат выпущен на оба имени до 10.12.2026, каталог
  `/etc/letsencrypt/live/mokeevasky.ru`.
- nginx переключён на выпущенные пути. Заодно заведён отдельный блок: `www`
  уводится на основное имя. Причина не в красоте адреса — вход хранится в cookie,
  а cookie привязана к имени, и вход под `www` был бы отдельным сеансом; выглядело
  бы это как случайный разлогин.
- Блок основного имени оставлен первым для порта 443: значит он же отвечает на
  обращения по адресу `46.8.178.196`. Там браузер предупредит о подлинности —
  так и должно быть, сертификат выдан на имена.

**Проверено** (запросами без `-k`, то есть с настоящей проверкой подлинности):
- `/Claude/` — 303 на вход, `/Claude/login` — 200, `/Claude/healthz` — 200;
- с провода: `CN = mokeevasky.ru`, издатель Let's Encrypt, в `subjectAltName`
  оба имени; `www` — 301 на `https://mokeevasky.ru/Claude/`, HTTP — 301 на HTTPS;
- то, от чего зависит продление через 60 дней, проверено отдельно и по делу:
  повторный прогон скрипта положил настоящий файл в webroot и забрал его по HTTP
  по каждому имени — 200, содержимое совпало; `certbot.timer` активен и включён.
  Перевыпуска при этом не случилось (`--keep-until-expiring`), попытка не потрачена.

**Что осталось за рамками:** почта в Let's Encrypt не указана — писем о сломавшемся
продлении не будет. Добавляется потом: `certbot update_account -m почта`.

## 10.09.2026 — Кнопка перезапуска приложения

**Статус:** выполнено
**Коммиты:** `b8dc6c8` — кнопка перезапуска

**Суть задачи:** сделать в интерфейсе кнопку перезапуска — закрыть пункт 4 техдолга,
чтобы правки кода `webui` можно было применять из браузера.

**Ход:**
- `services/restart.py`: перезапуск ставится в очередь и ждёт, пока не допишутся
  выполняющиеся ответы. Сразу перезапускать нельзя — остановка службы гасит
  контрольную группу вместе с запущенным ею `claude`, а ответ пишется в базу
  только по завершении.
- Драйвер считает выполняющиеся ответы (`claude_driver.active_runs()`).
- `POST /settings/restart` и `GET /settings/restart/state`; кнопка и опрос
  состояния в разделе «Настройки», страница сама обновляется по `healthz`.
- Новых разрешений sudo не потребовалось: `/usr/bin/systemctl restart webui`
  уже был в `infra/sudo-allowed.list`, команда вызывается дословно в таком виде.

**Проверено:**
- механика «перезапуск заказан процессом из своей же контрольной группы» — на
  подставной службе пользователя: процесс-заказчик убит (строка после вызова в
  журнал не попала), служба поднялась с новым PID. То есть задание systemd
  переживает гибель заказчика;
- стенд на отсрочку: при выполняющемся ответе служба не трогается, после
  завершения — перезапускается;
- тот же стенд на прежнем поведении (ожидание убрано) падает: «перезапуск оборвал
  бы выполняющийся ответ». Значит зелёный результат что-то доказывает;
- отрисовка страницы «Настройки»: кнопка, скрипт и адреса с префиксом `/Claude`;
- точки без входа отдают 401;
- **нажатием кнопки** 10.09.2026 в 20:50 и 20:54. В журнале службы
  `sudo … COMMAND=/usr/bin/systemctl restart webui` от приложения (PWD — рабочий
  каталог службы), дальше штатная остановка: `Waiting for connections to close` →
  `Application shutdown complete` → `Deactivated successfully` → `Started`.

**Техдолг:** пункт 4 закрыт, перенесён в раздел «Закрыто» с хэшем `b8dc6c8`.

## 10.09.2026 — Проект «Веб-интерфейс» в разделе «Проекты»

**Статус:** выполнено
**Коммиты:** `8ff9bbf` — проект «Веб-интерфейс»

**Суть задачи:** добавить в раздел «Проекты» сам веб-интерфейс, чтобы его можно было
дорабатывать из браузера, а не только из терминала.

**Ход:**
- Запись в таблице `projects`: «Веб-интерфейс», `slug = webui`, каталог
  `/home/mokeeva/main/webui`, `allow_bash = true`.
- Заводилось записью, а не формой «Новый проект»: форма транслитерирует имя в slug
  и создаёт по нему каталог — получился бы пустой `veb-interfeys/` рядом с `webui/`.
- `allow_bash` поднят: без запуска команд правку кода приложения нечем применить.
- Устройство и ограничения описаны в `architect.md`, итог — в `result.md`.

**Проверено:**
- список раздела «Проекты» тем же запросом, каким его набирает страница, — проект
  в списке вторым;
- движок запущен с параметрами этой темы, как это делает обработчик отправки:
  `pwd` дал `/home/mokeeva/main/webui`, версия FastAPI прочитана из `requirements.txt`
  (инструменты в ходе ответа: Bash, Glob, Grep);
- проверка прогнана на дефекте — те же параметры с `allow_bash = false`: Bash
  недоступен, команда не выполняется. Значит зелёный результат выше что-то доказывает;
- служба жива, `/Claude/healthz` отвечает 200, `/Claude/projects` без входа уводит
  на форму входа.

**Не проверено:** нажатием кнопки в браузере — для этого нужен вход, а пароль сменён
и мне неизвестен. Выпуск сессии в обход входа заблокирован. Прошу открыть
`https://46.8.178.196/Claude/projects` и убедиться, что проект на месте.

**Ограничение:** перезапуск `webui` из темы этого проекта обрывает собственный ответ —
`claude` живёт в контрольной группе `webui.service`. Перезапуск после правок — из
терминала.

**Техдолг:** с подтверждения заведены пункты 4 (перезапуск из темы) и 5 (`.env`
с ключами в рабочем каталоге проекта) в `tech_debt.md`.

## 10.09.2026 — Курс по аэродинамике

**Статус:** в работе
**Коммиты:** —

**Суть задачи:** создать курс по аэродинамике. Форма не задана; выбран статический
веб-курс в каталоге `samolet/` — профиль работ дизайн и вёрстка, а курс нужно читать
и проходить, а не собирать.

## 09.09.2026 09:55 — Резервное копирование базы

**Статус:** выполнено
**Коммиты:** `dce892b` — резервное копирование

**Суть задачи:** настроить резервное копирование базы `webui` — позиция 3 техдолга.

**Ход:**
- `infra/pg-backup.sh`: выгрузка текстовым SQL с gzip, роли отдельно, проверка
  каждой копии (размер, `gzip -t`, отметка о завершении, наличие таблиц),
  ротация 14 копий в хранилище и 3 в `exchange/backups`.
- `infra/pg-restore-check.sh`: разворачивает копию в отдельную базу и сверяет с
  рабочей число строк по таблицам, столбцов и индексов. Рабочая база не трогается.
- Расписание в crontab пользователя (root не нужен, `Linger=no` не даёт применить
  user-таймеры systemd): выгрузка ежедневно 03:17, проверка по воскресеньям 04:05.
  Копия расписания — `infra/crontab.mokeeva`.
- Формат выбран под сужённые права: `pg_restore` в разрешённых командах нет,
  а текстовую выгрузку принимает `psql`, который разрешён.

**Проверено:**
- восстановление на данных (100 сообщений, 200 метрик) — совпало по всем таблицам;
- проверка прогнана на трёх дефектах: обрезанная выгрузка (9 расхождений),
  потеря 30 строк из блока `COPY` (расхождение найдено), битый архив (код 1);
- ротация на 20 подставных файлах — осталось ровно 14 новейших;
- прогон в окружении, приближенном к cron (пустой env, минимальный PATH).

**Ошибка в собственном тесте:** первая попытка сымитировать потерю строк удаляла
строки `INSERT`, которых в выгрузке нет — `pg_dump` пишет данные блоком `COPY`.
Копия оставалась годной, и проверка верно это показала. Тест переделан.

## 09.09.2026 09:45 — Сужение беспарольного sudo

**Статус:** выполнено
**Коммиты:** `51bbd97` — сужение sudo

**Суть задачи:** сузить `sudo` с `NOPASSWD:ALL` до конкретных команд и завести файл,
в котором эти команды хранятся и пополняются по согласованию.

**Ход:**
- Заведён каталог `infra/`: `sudo-allowed.list` как источник истины, `apply-sudo.sh`
  для применения с проверкой через `visudo -c` до установки, `webui-deploy.sh`
  для раскладки конфигов nginx, `README.md` с порядком добавления команд.
- Разрешено 18 команд от root и 6 от postgres. Прежнее правило `NOPASSWD:ALL`
  убрано, копия сохранена в `/root`.
- Проверено нажатием: разрешённое работает (`systemctl is-active`, `nginx -t`,
  `apt-get update`, `psql` от postgres, `journalctl`, раскладка nginx), запрещённое
  отказывает — `sudo true`, `tee /etc/passwd`, `apt-get install`, `systemctl stop ssh`,
  `bash -c id`, `cp /etc/shadow`, `sed -i`, `chmod 777 /etc`, `visudo`.
- Отдельно проверен обход через `apt-get -o APT::Update::Pre-Invoke` — отклонён.
  Поэтому аргументы у `apt-get update` намеренно не разрешены: звёздочка там
  равносильна полному root.

## 09.09.2026 08:58 — Веб-интерфейс: реализация всех этапов

**Статус:** выполнено
**Коммиты:** `5e453ce` — все этапы проекта 1

**Суть задачи:** пройти все семь этапов плана из `exchange/tz_090926.md` — от установки
инфраструктуры до раздела «Настройки».

**Ход:**
- Этап 0. Установлены PostgreSQL 16, nginx 1.24, окружение Python. Создана база и роль
  `webui`, самоподписанный сертификат на IP, конфиг nginx с TLS и ограничением частоты
  входа, юнит `webui.service` в автозапуске. Проверено снаружи: `https://46.8.178.196/Claude`
  отвечает, HTTP уводит на HTTPS.
- Этап 1. Схема из девяти таблиц, применяется идемпотентно при старте. Пароли argon2id,
  серверные сессии, обязательная смена пароля при первом входе, проверка стойкости,
  защита от перебора. Сценарий проверен 11 шагами: от отказа на слабом пароле до
  блокировки после серии неудач.
- Этап 2. Дизайн-система в духе 1С на CSS-токенах, светлая и тёмная темы, компоненты:
  командная панель, таблицы, формы, плитки метрик, метки, уведомления.
- Этап 3. Дашборд: процессор, память, диск, нагрузка, сеть, аптайм, процессы;
  автообновление раз в 10 с с настраиваемым интервалом; график за сутки на canvas;
  учёт расхода токенов. Числа сверены с `free -m`, `df` и `/proc/loadavg` — совпадают.
- Этап 4. Чатики: список, создание, переименование, удаление, поток ответа через SSE,
  автоназвание по первой реплике, продолжение диалога через `--resume`.
- Этап 5. Проекты: темы, диалог с доступом к каталогу проекта, файлообмен.
  Проверено реальной задачей — агент создал и отредактировал файл.
- Этап 6. Настройки: параметры приложения с проверкой значений, редактор файлов
  сопровождения с предпросмотром, активные сессии, права проектов.
- Идеи 1-6 реализованы: предупреждение о месячном пределе, полнотекстовый поиск по
  диалогам, экспорт в markdown, `log.md` лентой задач, тёмная тема, график истории.

**Найдено и исправлено при проверке нажатием:**
- `--allowed-tools` не запрещает остальные инструменты — Bash выполнялся при
  `allow_bash=false`. Добавлен явный `--disallowed-tools Bash BashOutput KillShell`.
  Повторная проверка: команда отклонена, правка файлов сохранилась.
- Модель бралась произвольным первым ключом `modelUsage` — в базу попадал `haiku`
  вместо `opus`. Теперь берётся модель с наибольшим числом токенов на выход.
- Slug проекта сохранял кириллицу, каталог получался `/home/mokeeva/main/проба`.
  Добавлена транслитерация.

**Технический долг:** по подтверждению заказчика в `tech_debt.md` внесены три позиции —
самоподписанный сертификат, отсутствие изоляции приложения от прав пользователя,
отсутствие резервного копирования базы.

## 09.09.2026 08:25 — Проект 1: веб-интерфейс для совместной работы

**Статус:** разбор выполнен, реализация ожидает подтверждения
**Коммиты:** —

**Суть задачи:** разобрать ТЗ из `exchange/tz_090926.txt`, изложить план работ, вопросы
и идеи в `exchange/tz_090926.md`, ждать ответов.

**Ход:**
- Собрано состояние сервера: Ubuntu 24.04, 4 CPU, 2.9 ГБ RAM, 41 ГБ свободно;
  из нужного установлен только Python 3.12.3, порт занят лишь 22.
- Цены и модели Claude API, а также возможности по лимитам токенов сверены со
  справочником, а не по памяти.
- Составлен план: 7 этапов от инфраструктуры до раздела «Настройки», с критериями
  готовности по каждому.
- Выявлены три блокера: нет прав на установку пакетов, пароль уходит по открытому HTTP,
  нужен API-ключ Claude. Записаны в `tz_090926.md` с вариантами решения.
- Уточнений по ТЗ — шесть, включая расхождение «три раздела» против четырёх
  перечисленных и глубину раздела «Проекты».
- Получены ответы заказчика. Встречные вопросы (достаточно ли подписки Pro; зачем
  нужен SDK, если задача — заменить терминал) разобраны в `exchange/ant.md`.
- По итогам разбора архитектура пересмотрена: вместо Messages API через `anthropic` SDK
  движком становится сам Claude Code в неинтерактивном режиме — задача не «ещё один
  чат», а веб-пульт к терминалу. Факты по Agent SDK и аутентификации сверены с
  документацией code.claude.com, не по памяти.
- СУБД по решению заказчика — PostgreSQL 16 вместо MariaDB.
- Заказчик выбрал вариант А (веб-интерфейс поверх Claude Code в неинтерактивном режиме),
  подтвердил личный характер инструмента без сторонних пользователей.
- Включён беспарольный `sudo` — блокеры 3.1 и 3.2 сняты, этап 0 разблокирован.

## 09.09.2026 07:38 — Первичная настройка репозитория

**Статус:** выполнено
**Коммиты:** `b702fd0` — первичная настройка, `c27b149` — хэш в log.md, `b5c5ece` — remote на GitHub

**Суть задачи:** дать SFTP-доступ к рабочей папке, разобрать `CLAUDE.md`, завести
файлы сопровождения, инициализировать git и подготовить подключение к GitHub.

**Ход:**
- Починен SFTP: на сервере отсутствовал ed25519 host-ключ, из-за чего клиент FAR
  (движок PuTTY) не мог согласовать host key алгоритм и падал на «Connecting to host».
  Выполнен `ssh-keygen -A`, ключи перечитаны после перезапуска `ssh.socket`/`ssh.service`.
- Стартовый каталог SFTP переведён на `exchange/` через
  `Subsystem sftp internal-sftp -d %d/main/exchange` — правка применена 09.09.2026 08:53,
  после включения беспарольного `sudo`; бэкап конфига
  `/etc/ssh/sshd_config.bak-090926-085327`. Проверено: `pwd` даёт
  `/home/mokeeva/main/exchange`, `cd ..` работает, shell-вход не затронут.
- Вопросы по `CLAUDE.md` заданы в `exchange/quest_090926.md`, ответы получены.
- Заведены файлы сопровождения: `architect.md`, `result.md`, `current_questions.md`,
  `tech_debt.md`, `log.md`.
- Инициализирован git, добавлен `.gitignore`.
- Сгенерирован ключ для GitHub, публичная часть выложена в `exchange/git.txt`.
- `CLAUDE.md` приведён в соответствие с полученными ответами.
- Подключён remote `origin` (`git@github.com:Svetlana-claude/Main.git`), доступ по SSH
  проверен (аккаунт `Svetlana-claude`), первый push выполнен.
