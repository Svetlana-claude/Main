# Разрешённые беспарольные команды sudo
#
# ЭТОТ ФАЙЛ — ИСТОЧНИК ИСТИНЫ. Правится здесь, затем применяется:
#     sudo bash infra/apply-sudo.sh
#
# Новая команда добавляется только по согласованию: агент предлагает строку и
# объясняет, зачем она, вы разрешаете — и только тогда она появляется здесь.
#
# Формат: одна команда на строку с полным путём. Аргументы учитываются:
# запись «systemctl restart webui» разрешает ровно эту команду и не разрешает
# «systemctl restart ssh». Звёздочка — glob по остатку строки, ставить с оглядкой.
#
# Раздел задаёт, от чьего имени выполняется команда.
# Пустые строки и строки с # игнорируются.
#
# ВАЖНО, ЧТО ЭТО НЕ ДАЁТ. Сужение защищает от случайной ошибки — опечатка в
# команде больше не выполнится от root. Границей безопасности от намеренного
# действия оно не является: любая возможность писать в /etc или ставить пакеты
# от root эквивалентна root. Правка systemd-юнитов сюда сознательно не включена —
# юнит исполняется от root, его подмена равносильна root.
# Полный root с паролем у вас остаётся: mokeeva входит в группу sudo.
#
# Про установку пакетов (решение от 11.09.2026). Раньше её здесь не было вовсе.
# Теперь она разрешена — но не командой apt-get, а обёрткой webui-apt-install,
# которая пропускает только имена пакетов. Причина: у apt-get есть параметр
# -o ...::Pre-Invoke, выполняющий произвольную команду, и довод-путь к локальному
# .deb, чьи сценарии идут от root, — запись «apt-get install *» была бы просто
# беспарольным root. Обёртка эти два хода закрывает; при этом ставить пакеты из
# репозиториев Ubuntu агент теперь может сам, и это осознанное расширение доверия.


# === root ===

# --- Управление службами проекта. Перечислены поимённо: разрешено
# --- перезапустить webui и nginx, но не, скажем, отключить ssh.
/usr/bin/systemctl daemon-reload
/usr/bin/systemctl restart webui
/usr/bin/systemctl start webui
/usr/bin/systemctl stop webui
/usr/bin/systemctl reload nginx
/usr/bin/systemctl restart nginx
/usr/bin/systemctl restart postgresql

# --- Опрос состояния: только чтение, ничего не меняет
/usr/bin/systemctl status *
/usr/bin/systemctl is-active *
/usr/bin/systemctl is-enabled *
/usr/bin/systemctl show *
/usr/bin/systemctl list-units *
/usr/bin/systemctl list-unit-files *
/usr/bin/systemctl cat *

# --- Проверка конфигурации nginx перед перезагрузкой
/usr/sbin/nginx -t

# --- Чтение журналов служб
/usr/bin/journalctl

# --- Обновление списка пакетов. Аргументы намеренно не разрешены: вызывать
# --- строго «sudo apt-get update», без флагов. Звёздочку здесь ставить нельзя —
# --- у apt-get есть параметр -o APT::Update::Pre-Invoke, через который
# --- выполняется произвольная команда от root, то есть «apt-get update *»
# --- равносильно полному root. Сама установка идёт не отсюда, а обёрткой ниже.
/usr/bin/apt-get update

# --- Раскладка конфигов nginx из webui/deploy с проверкой и перезагрузкой.
# --- Скрипт принадлежит root и недоступен на запись пользователю, иначе
# --- разрешение на него означало бы разрешение на любой код от root.
# --- То же верно и про две обёртки ниже; ставятся они одной командой:
# ---     sudo bash infra/install-root-helpers.sh
/usr/local/sbin/webui-deploy

# --- Установка пакетов из репозиториев Ubuntu. Обёртка над apt-get, пропускает
# --- только имена пакетов — почему именно так, см. примечание в начале файла.
/usr/local/sbin/webui-apt-install

# --- Выпуск и продление сертификата Let's Encrypt (копия infra/tls-letsencrypt.sh),
# --- а также смена почты учётной записи: «webui-tls-issue --email адрес@почта».
# --- Пишет в /etc/letsencrypt и ставит certbot, то есть это тоже полноценный
# --- root-шаг; сужен он тем, что делает ровно одно дело и лежит в файле,
# --- недоступном на запись пользователю.
# ---
# --- Почему смена почты сделана режимом обёртки, а не строкой «certbot
# --- update_account *»: у certbot есть --pre-hook и --deploy-hook, выполняющие
# --- произвольную команду от root, и --config-dir, которым можно писать от root
# --- куда угодно, — звёздочка разрешила бы и это. Обёртка берёт ровно один
# --- довод и проверяет его на вид адреса, так что флаг в него не спрятать.
/usr/local/sbin/webui-tls-issue

# --- Управление VPN-сервером WireGuard (решение от 12.09.2026): установка,
# --- выдача и отзыв клиентов, состояние. Обёртка над копией скриптов
# --- vpn-server/, лежащей в /usr/local/lib/webui-vpn под root.
# ---
# --- Почему обёртка, а не «bash vpn-server/install-wireguard.sh» в списке:
# --- каталог vpn-server/ доступен на запись пользователю и агенту, и запись
# --- вида «/bin/bash /home/mokeeva/main/vpn-server/*» была бы просто
# --- беспарольным root — достаточно переписать скрипт. Обёртка запускает
# --- root-овую копию и перед запуском проверяет её владельца и права.
# ---
# --- Набор команд закрыт: install, add, remove, list, status. Произвольные
# --- доводы не проходят — имя клиента сверяется с образцом, порт обязан быть
# --- числом, каталог выдачи задан жёстко. Флаг или путь в них не спрятать.
# ---
# --- ЧЕГО ЭТО НЕ СКРЫВАЕТ. Установка пишет в /etc и правит iptables, то есть
# --- это полноценный root-шаг, сужённый до одного дела; а выданный конфиг
# --- клиента лежит в exchange/vpn с закрытым ключом, так что доступ к учётной
# --- записи mokeeva означает и доступ к самому VPN.
/usr/local/sbin/webui-vpn


# === postgres ===

# Работа с базой от имени postgres, а не root: даёт полный доступ к СУБД,
# но не к системе.
/usr/bin/psql
/usr/bin/createdb
/usr/bin/createuser
/usr/bin/dropdb
/usr/bin/pg_dump
/usr/bin/pg_dumpall
